# Plan 012 Evidence

- Final implementation commit: `d1c1214`
- Branch: `codex/gameplay-roadmap`
- Date/time zone: `2026-07-10T16:58:11-06:00` (America/Denver)
- Source authority: installed Stratagus `b3b4babe8eb8f908ac9cb25da9c0fd83c67957c5`

## Player-visible scenarios

| Scenario | Result | Evidence |
|---|---|---|
| M01 replay | READY | The post-fix in-app session again began with one selected Peasant, no Hall, and `10000/5000/5000`; the Peasant reached the site, paid once, and completed the real Hall. Plan 011's exact six-retask, arrival, cancel, release, and `75%` refund evidence remains READY at `plans/evidence/011.md`. |
| M02 temporary congestion | READY | The rear unit retained its exact nonempty Move for 12 blocked ticks, never overlapped or held a live empty path, and completed at tick 88 after the front unit moved. Maximum retry update: `1.8ms`. |
| M03 blocked click | READY | The isolated first-ring candidate was rejected; the mover selected reachable tile `(4,4)` at the minimum Chebyshev range `1`. The 48x48/64x64 samples stayed below the shared `20ms` route budget in the accepted run. |
| M04 source formation | READY | Five Footmen preserved exact integer source offsets: `(3,3)->(9,7)`, `(4,3)->(10,7)`, `(5,3)->(11,7)`, `(4,2)->(10,6)`, `(4,4)->(10,8)`. All five completed with zero drops, empty-path ticks, or overlaps; semantic replay matched. Issue/max update: `1.2ms/1.4ms`. |

## Crowded-base regression

The first real crowded session exposed a missing mixed-obstruction case: a
formation slot blocked by a building while the assigned Footman was temporarily
surrounded by mobile teammates. The focused RED result was:

- immediate path length `1`;
- moved/settled `4/4`;
- one premature drop;
- the affected Footman remained on its source tile.

Commit `d1c1214` separates permanently stationary occupancy from mobile
congestion during the fallback reachability search. The same fixture then
required and passed:

- initial affected order still live with path length greater than one;
- moved/settled `5/5`;
- zero premature drops;
- zero same-layer overlap ticks.

## In-app playable acceptance

One Peasant built a Hall, two Farms, and a Barracks through the real UI, then
trained five Footmen through the real queue. In the accepted non-sealed layout:

- all five crossed the open Barracks exit and settled in open ground;
- all five moved to reachable tiles around the occupied Hall;
- all five traversed back through the crowded Hall/Farm/Barracks area;
- repeated right-clicks preserved a recognizable relative shape with no lost
  unit, permanent stack, jump-through, or visible command hitch;
- the 1024x768 input repeat also ended with five distinct selected, idle
  Footmen.

Final 1024x768 world tiles were distinct: `(116,118)`, `(115,116)`,
`(116,116)`, `(116,119)`, and `(116,117)`. The smoke state reported exactly
five selected Footmen, `firstSelectedOrderKind: null`, and average
update/render of `1.66ms/8.03ms`.

## Verification and resource discipline

- `./node_modules/.bin/tsc --noEmit` — PASS
- `npm run verify:source-pathfinding` — PASS
- `npm run verify:source-formation-movement` — PASS
- `npm run verify:browser-fixed-demo-input` — PASS with M02-M04 and the crowded blocked-slot regression
- `npm run verify:runtime-determinism` — PASS
- `git diff --check` — PASS
- In-app Browser at 1280x720 and 1024x768 — PASS
- Earlier Plan 012-C playable-map sweep — 12/12 PASS; the correction rerun was bounded at 7/12 with no failure before manual acceptance superseded it

The current exact M01 command-line harness attempt timed out waiting for its
temporary local server, before loading the game. It left no browser or server
process behind. This is recorded as harness noise rather than a gameplay pass;
the current in-app Hall build and the unchanged exact Plan 011 M01 packet are
the acceptance evidence.

## Review decision

- READY
- Reviewer: `/root/plan012_low_resource_review`
- Scope: read-only review of `01f2651...d1c1214`; no browser, server, build, or test process was launched by the reviewer
- Findings: none

Review decision: READY
