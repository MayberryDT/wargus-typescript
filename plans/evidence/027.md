# Plan 027 — verification-gate repair acceptance

## Scope and status

Status: DONE-VERIFIED. Branch-local Steps 2–3, the accepted Plan 026 controller dependency, two separately scoped portability follow-ups, coordinator browser revalidation, historical-contract revalidation, retained artifacts, and exact cleanup proof are complete. No gameplay, UI, renderer, manifest, or asset behavior changed.

## Original-source decision

The plan's authoritative pinned source hash was unavailable upstream. Per the automation-first directive, this branch used the repository-documented, read-only installed engine checkout:

- root: `/home/halla/workspaces/Wargus-TypeScript-artifacts/original-stratagus-b3b4babe`
- revision: `3526da2a93b253394e28fff8d3546146e256328b`
- preflight: `src/ui/mouse.cpp`, `src/ui/interface.cpp`, and `src/ui/mainscr.cpp` were readable.

No files in that checkout were modified.

## RED baselines

`npm run verify:source-resource-ui` failed before repair with the expected `ENOENT` for `/home/tyler/Documents/Codex/2026-04-24/files-mentioned-by-the-user-setup/stratagus-src/src/ui/mouse.cpp` at `verify-source-resource-ui.mjs:95`.

`npm run verify:fixed-demo-polish` failed before repair with exactly:

```text
Movement should continue to consult passability before entering a tile.
Browser startup should render a full-screen loading layer.
Fixed demo polish verification failed (2 errors).
```

## Portability checkpoint

- `env -u WARGUS_ORIGINAL_SOURCE_ROOT npm run verify:source-resource-ui` failed explicitly: `WARGUS_ORIGINAL_SOURCE_ROOT must name a readable Stratagus source root.`
- `WARGUS_ORIGINAL_SOURCE_ROOT=/tmp/plan027-missing-source npm run verify:source-resource-ui` failed explicitly and named `/tmp/plan027-missing-source/src/ui/mouse.cpp`.
- `WARGUS_ORIGINAL_SOURCE_ROOT=/home/halla/workspaces/Wargus-TypeScript-artifacts/original-stratagus-b3b4babe npm run verify:source-resource-ui` passed: `Verified 6 source resource UI slots and browser assets.`

The verifier has no Tyler-path fallback. It requires a nonblank `WARGUS_ORIGINAL_SOURCE_ROOT` and verifies read access to all three required UI files before reading any of them.

## Stale-contract checkpoint

- `stepMoveOrder` is now required to call `isUnitFootprintPassable(...)` exactly twice in its live body and not directly call `isTilePassable(...)`.
- The loading-screen check now requires `LoadingScreenState`, `createTrackedContainer()`, visible loading-layer state, progress rendering, user-facing first-load text, and `showLoadingError`.
- `npm run verify:fixed-demo-polish` passed.

## Branch-local verification

| Gate | Result |
| --- | --- |
| `verify:source-resource-ui` with approved root | PASS |
| `verify:fixed-demo-polish` | PASS |
| `verify:source-pathfinding` | BLOCKED before assertions by its own Tyler-only `pathfinder/pathfinder.cpp` literal; not edited or retried under Plan 027's STOP rule. |

The pathfinding verifier's current static contract was inspected read-only: it already requires the same two `isUnitFootprintPassable(...)` gates and no `isTilePassable(...)` call within `stepMoveOrder`.

## Diff guard and commits

Before this evidence file, the branch diff from the assigned base contained only:

- `scripts/verify-source-resource-ui.mjs`
- `scripts/verify-fixed-demo-polish.mjs`

Implementation commits:

- `3ba66c5be31b902b14afb8894293a10f9ff1a004` — Make source resource UI verification portable
- `7dab337606d015d2eb0b497b958e6fdc7b910f29` — Refresh fixed demo polish contracts

## Coordinator integration and separate repairs

Plan 026 was accepted before browser work: controller implementation `eb0cb0e`–`02051ad`, coordinator integration `d99a24a37d273f98b37c78e2ea4f750513b8abfe`, acceptance evidence `9bbcc00`, and roadmap closeout `402c370`.

The reviewed Plan 027 branch checkpoints integrated as:

- `ef7d8a8` — portable source-resource UI verification;
- `6b8f89a` — current fixed-demo movement/loading contracts;
- `c0d18b7` — branch evidence checkpoint.

Read-only revalidation then exposed two additional Tyler-only source paths outside the original branch ownership. The coordinator followed the STOP rule and repaired each in a separate isolated worktree with independent review:

- source pathfinding: branch `e821136`, integrated as `caa7df8`; mandatory checked `WARGUS_ORIGINAL_SOURCE_ROOT` for `src/pathfinder/pathfinder.cpp`, `src/pathfinder/astar.cpp`, and `src/action/action_move.cpp`; independent review approved with zero findings;
- source viewport mode: branches `6de1766` and `6641e63`, integrated as `de8d83f` and `1b8ad44`; mandatory checked root for `src/ui/ui.cpp` and `src/include/ui.h`, plus exactly one stale pointer fragment replaced by the current translated-point dispatch/delegation contract; independent review approved with zero findings.

Unset-root and missing-file cases fail actionably; no repaired verifier contains a Tyler path, fallback, or silent skip.

## Browser revalidation

The fresh video/render context could read `/dev/dri/card1` and `/dev/dri/renderD128`. Preflight recorded MemAvailable about 9.2 GiB, swap used about 308 KiB, workspace free about 350 GiB, load `0.58 0.78 0.71`, and no conflicting Wargus verifier process. Ports `54991`, `55000`, and `55001` were inspected and unused.

### Runtime smoke

- Requested candidate: `WARGUS_BROWSER_RUNTIME_PORT=54991`.
- Controller allocation: server `54991`, debug reservation `55000`.
- Result: `Browser runtime smoke verified (plan014, one tab, port 54991, wall 10.3s; canvas 1280x720/332 colors; live AI player 1, script 8, launches 0, update 7.42ms).`
- Cleanup: controller reported exact-owned-PID cleanup and port `54991` clear; both `54991` and `55000` were independently confirmed clear after exit.

### Browser-native viewport

The full package gate passed: responsive canvas, resize sync, source keep-ratio title shell, source video fullscreen, and source viewport mode.

### Modern HUD

- Controller allocation: server `55000`, debug `55001`.
- Recorded owned roots: Vite/npm root `2028361` with Vite descendant `2028374`; Chrome root `2028408` with the sampled descendant tree rooted at `2028422` and `2028423`.
- Result: all `1000x720`, `1280x720`, and `900x640` scenarios passed.
- Cleanup: every recorded owned root/descendant, both ports, the verifier process, and Chrome profile `/tmp/wargus-modern-hud-chrome-RP7fxd` were independently confirmed absent after exit. Pre-existing listeners were untouched.

Generated screenshots were preserved outside Git under `.artifacts/verification/027/caa7df8ece5952da36f3d608c640b29c3fda66a8/20260729T041937Z/` in the retained artifact workspace. SHA-256 values:

- `wargus-modern-hud-1000x720.png`: `a259ac48e4bc02fd1b1c08969b7793e91783b0381e50bdf0c75a141be3cecb59`
- `wargus-modern-hud-1280x720.png`: `3cf7d03d208f41ee760db5201550338cf82f34e99fd3da1ee5bfc0616d9d8007`
- `wargus-modern-hud-900x640.png`: `8750b6f7ffd91ccf095c9296a6dc1afd906bef4175865eeef177de795e48a594`

Post-run metrics remained healthy: MemAvailable about 9.3 GiB, swap used about 308 KiB, workspace free about 350 GiB.

## Historical-contract revalidation

| Plan | Focused contract | Result |
| --- | --- | --- |
| 003 | source-resource UI remains in the project gate | PASS |
| 005 | fixed-demo polish assertions match current behavior contracts | PASS |
| 006 | source pathfinding and orders extraction seam | PASS |
| 007 | browser runtime smoke under the integrated controller | PASS |
| 009 | browser-native viewport and harness contract | PASS |
| 010 | primary live modern-HUD layout at three viewports | PASS |

Runtime smoke and modern HUD passed at `caa7df8`. The later `de8d83f` and `1b8ad44` commits changed only the source-viewport verifier; the full browser-native viewport package gate and all non-browser Plan 027 gates were rerun and passed at `1b8ad44`. Final focused commands:

```text
WARGUS_ORIGINAL_SOURCE_ROOT=<pinned-root> npm run verify:source-resource-ui
npm run verify:fixed-demo-polish
WARGUS_ORIGINAL_SOURCE_ROOT=<pinned-root> npm run verify:source-pathfinding
WARGUS_ORIGINAL_SOURCE_ROOT=<pinned-root> npm run verify:browser-native-viewport
WARGUS_BROWSER_RUNTIME_PORT=54991 npm run verify:browser-runtime-smoke
npm run verify:modern-hud-layout
```

Verdict: Plan 027 is accepted as `DONE-VERIFIED`. No performance budget or product behavior changed.
