# Plan 014 Evidence

- Branch: `codex/gameplay-roadmap`
- Planned baseline: `6af2eebb71732a76ae36dfeb56a570c854634ab0`
- Original implementation/evidence head reviewed: `7de113cd152acf3185e9d76d85d2472977c17870`
- Independent review: `/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-final-review-report.md`
- Review-fix re-review: `/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-review-fixes-rereview-report.md`
- Review decision: **READY — explicit user acceptance override, 2026-07-24**
- Review-fix commits:
  - `78e128d` — shared source-style construction requests, pending-count/resource reservation, and real manager fixture
  - `0224d12` — exact force membership, retryable orderless launches, and script-bounded save normalization
  - `fedb8f5` — declared Playwright runtime, real Vite preview mode, and restored focused browser basics
  - `154efa3` — live AI evidence, production-honest Plan 014 smoke assertions, and JSON report hook
  - Current changeset — AI-wide unpaid-build reservation boundary, atomic multi-unit launch, and declared save-schema scope
- Date/time zone: 2026-07-24, America/Chicago
- Execution host/worktree: `halla` / `/home/halla/workspaces/Wargus-TypeScript-roadmap`
- Current decision: **DONE — READY**. The user explicitly accepted the ordinary
  core-loop retry combined with preserved live progression and reviewed fix
  evidence, superseding exhaustive Gate A/Task 9 segmentation, verifier
  expansion, same-session 4/16-wave proof, and a separate independent READY
  review. The waived evidence is identified below rather than claimed as run.

## Evidence correction

The earlier M08/M09 browser row was generated by preseeded synthetic fixtures,
not by live fixed-demo progression. It remains useful focused evidence for the
interpreter, difficulty normalization, and save compatibility, but it does not
prove the one-Peasant opening built through the Hall and two production
buildings or delivered literal 1/4/16 pressure. The browser's Plan 014 mode no
longer calls either synthetic fixture or labels their launch counts as live.

The earlier M07 record is also contradictory. The dedicated report says the
corrected browser replay was not launched, while the closeout summary reports a
PASS on port 55741. With no preserved raw final payload, M07 is treated as **not
accepted** until a reproducible rerun captures the full result. M01 and M04
remain accepted from their focused reports.

The opening performance profile is credible for the sampled one-Peasant stage:
4.773821 ms average update and 19.079281 ms average render at its final sample.
It does not cover a built base or staged pressure, so progressed-stage 20/24 ms
evidence remains pending.

## Review-fix implementation

### Construction manager

Every scripted and direct Hall/supply/Barracks/lumber/blacksmith/tech/shipyard
construction request now uses one shared path. It counts completed buildings,
foundations, and unpaid `build` orders in `phase: "to-site"`; reserves every
valid unpaid order's cost before accepting another; and deterministically
retasks source-eligible workers travelling to a resource without stealing
build, oil-platform, repair, or active-gather work. Resources are still deducted
only when the builder reaches the site.

The same AI-only available-resource view now gates every immediate manager
spend: worker and combat-unit training, producer upgrades, research, and later
construction requests all see raw player resources minus every valid unpaid
`build`/`to-site` reservation. Human queue and research actions continue to use
raw resources and are not blocked by this AI policy.

`verify:plan014-ai-runtime` runs the production manager against real fixed-demo
world state for the one-Hall opening, a second Barracks with only a travelling
worker available, and competing unpaid costs. A second-Barracks reservation
fixture keeps an active Hall, Barracks, and Blacksmith from spending in both the
issuing think and a later think, advances the real build order to a foundation
without cancellation, then confirms production and research can resume after
post-arrival funds are supplied.

### Scripted forces and saves

Force targets now use exact race-resolved unit types, and assignment claims an
id once across target rows. Launch plans every assigned member's exact target,
destination, and path without mutation; only a completely feasible plan is
applied and verified before launch history is appended and the slot detaches.
A mixed feasible/impossible fixture proves a failed launch preserves both
members' prior orders, the force slot, and empty launch history before a
successful two-member retry creates exactly one record. The existing focused
coverage still includes the cavalry-mage/mage composition and no-pressure path.

Save normalization replays the selected source script to bound script index,
force slots, targets, roles, assigned ids, launches, and per-launch ids. It
filters missing or wrong-type active ids, deduplicates them deterministically,
caps launch history to the script, and prevents active/launch overlap. The
runtime verifier mutates a save with oversized/stale force state and loads it
through the real save loader before checking those bounds.

### Honest browser gate and live evidence

Playwright `1.61.1` is pinned in `package.json` and the lockfile. Production
smoke launches Vite `preview`; development smoke launches the development
server. Chromium resolves from Playwright unless `CHROME_BIN` is explicitly
provided, so the command no longer embeds Halla paths or an undeclared external
module. A focused basics shard retains audio/music, fog, selection,
right-click, and screenshot-transition checks within the browser time budget.

The smoke surface now publishes bounded live AI evidence from the running
world: exact force targets and assigned/ordered units, detached launches,
desired/completed/foundation/in-flight build counts, pending unpaid orders and
reserved resources, difficulty factors, AI-owned exploration/scout state,
production queues with total/remaining train seconds, and foundations with
total/remaining build seconds. Plan 014 browser mode validates this live shape;
the synthetic script and knowledge fixtures remain focused unit/integration
checks only.

For a separately authorized browser run,
`WARGUS_BROWSER_RUNTIME_REPORT=/absolute/path.json` writes the accepted final
payload after assertions. The versioned JSON includes the exact M01/M04/M07
fixture result when selected, live tick/pause/speed and AI evidence, and the
update/render sample. This is the reproducible artifact hook for reconciling
M07 and preserving real M09 duration/progressed-performance evidence; it does
not replace the visible Task 9 session.

## Scenario status

| Contract | Status | Current evidence |
|---|---|---|
| M01 construction replay | ACCEPTED | Prior focused browser replay covers unpaid travel, arrival payment, hidden builder, cancellation, release, and 75% refund; the non-browser manager fixtures add duplicate, AI-wide spend-reservation, and uncancelled-arrival coverage. |
| M04 movement replay | ACCEPTED | Prior focused browser replay completed the five-unit formation without order loss. |
| M07 combat replay | ACCEPTED BY USER OVERRIDE | The contradictory historical replay was not reconstructed. Corrected focused combat coverage remains green, and the user explicitly waived a new M07 browser replay for this core-loop closeout. |
| M08 live staged pressure | ACCEPTED BY USER OVERRIDE | The fresh retry visibly reached the one-Peasant opening, Hall/Farm completion, Barracks construction, a second worker, AI Hall, and AI Barracks. Preserved live P13 evidence adds player Barracks/Footman completion and a real one-Grunt launch/contact. Same-session 4/16 waves are explicitly waived and not claimed. |
| M09 timing/knowledge | ACCEPTED BY USER OVERRIDE | The retry visibly selected level 3, used Speed/Preferences, and proved Pause/Resume. Source-backed factor checks remain green; preserved live evidence adds a real duration and owner-unexplored scout assignment/return. The full difficulty sweep is waived and not claimed. |
| 20/24 ms budgets | ACCEPTED FOR OVERRIDE SCOPE | The retry's final live diagnostic reported 3.6 ms update / 5.2 ms render with the match responsive. Preserved progressed tick 8380 reported 12.446 ms average update / 3.674 ms average render after base, Footman, launch, and contact milestones. |

## Static verification

All project commands run sequentially, at low priority, on Halla. The final
review-fix verification matrix and exact PIDs are recorded in
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-review-fixes-report.md`.
The follow-up transaction-fix matrix is recorded in
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-atomic-fixes-report.md`.

- `./node_modules/.bin/tsc --noEmit` — PASS.
- `npm run verify:plan014-ai-runtime` — PASS; real manager/arrival, AI-wide spend reservation, exact mixed force, atomic failed-launch retry, and bounded loaded save state.
- `npm run verify:fixed-demo-random-ai` — PASS.
- Source-backed `npm run verify:source-ai-difficulty` — PASS.
- `npm run verify:source-ai-forces` — PASS; 74 plans across 40 campaign maps.
- Source-backed `npm run verify:source-ai-explores` — PASS.
- `npm run verify:save-schema` — PASS; 50 persisted `WorldState` fields.
- `npm run verify:minimap-render-cache` — PASS.
- `npm run verify:playtest-telemetry` — PASS.
- `npm run verify:runtime-determinism` — PASS; no random/wall-clock APIs under `src/**/*.ts`.
- `node --check` for the Plan 014 runtime, browser-runtime, and browser static-contract verifiers — PASS.
- Production-browser static contract — PASS.
- `npm run build` — PASS; 764 modules transformed after the asset preflight.
- `npm run verify:wargus-assets` — PASS; 1,182 files checked.

### Current transaction-fix verification

The focused verifier failed before production changes with Hall/research spend
against a travelling Barracks reservation, arrival cancellation, and a leaked
first-member attack order from a failed two-member launch. After the fixes, the
final current-worktree commands pass:

- `npm run verify:plan014-ai-runtime` — PASS.
- `./node_modules/.bin/tsc --noEmit` — PASS.
- `npm run verify:source-ai-forces` — PASS after replacing its obsolete
  non-atomic fragment check with plan/apply/verify/record ordering checks.
- `npm run verify:save-schema` — PASS; the verifier is now explicitly listed in
  Plan 014 scope and its drift command.
- `npm run verify:fixed-demo-random-ai` — PASS.
- `npm run verify:runtime-determinism` — PASS.

No browser, game, Vite server, Task 9 segment, deployment, push, or Plan 016/017
work is authorized in this fix task.

## Ground-scout eligibility correction

The immutable P08 save was loaded without simulation at tick 6876 and proved a
persistent eligibility failure: player 1 was `wc2-land-attack` at instruction
18 with `nextScoutTick=0`, six gold-mining Peons, and one idle Grunt reserved
for force 1, but no scout flyer or producer capable of creating one. The source
analysis remains at `/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-p08-source-state-analysis.md`.

Installed Stratagus `src/ai/ai_plan.cpp:405-500` chooses one idle movable
explorer, gives flyers priority, and records the exploration cycle after issuing
the order. The TypeScript manager now keeps its existing flyer path first, then
borrows one deterministic safe land unit only when no normal scout can act and
the AI owner's buffer still contains an unexplored tile. Selection prefers an
idle non-force military unit, then an idle worker without a repair target, then
one empty-handed resource worker. Active force members, builders, repairers,
trainers, transports, tankers, other players, and an existing explorer remain
ineligible. The fallback uses the existing explore-order provenance and
five-second cadence; a borrowed worker completes one destination and returns to
the ordinary idle economy path without a persistent scout role.

`npm run verify:plan014-ai-scout` failed before the production change at PID
2315963 with zero selected scouts, all six miners unchanged, no provenance, and
`nextScoutTick=0`. The final fixture passes the exact six-miner/one-force-Grunt
case, flyer and land priority, one-scout/no-duplicate behavior, exact owner-buffer
provenance, worker return to harvesting, unchanged human Explore admission, and
a bounded one-scout save/load round trip.

Final browser-free gates passed under root PID 2340370: Plan014 AI runtime
including the new scout verifier and scheduler, source AI explores, runtime
determinism, fixed-demo random AI, save schema, TypeScript, 1,182-asset
verification, production build (765 modules), and `git diff --check`. The
scheduler fixture remained inside its unchanged UI-yield bound at 31.877 ms.
No browser, server, game page, deployment, push, save-schema change, or
checkpoint mutation occurred. Full implementation evidence is under
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-scout-fix/`.
At that historical checkpoint, Plan014 remained **IN PROGRESS — NOT READY**
pending runtime continuation from the unchanged immutable P08/A lineage and the
then-current Task 9/READY gates.

## Depot-first mining and near-depot exploration correction

The accepted P29 and P31 checkpoints preserve five 100-gold Peons on valid,
progressing paths from mine 3 at `(3952,624)` to completed Hall 18 at
`(3808,3712)`. Mine 14 at `(3600,3824)` is live and pathable but its AI-owner
exploration byte remains zero in both checkpoints, so it is correctly excluded
from resource selection. The source ruling is installed Stratagus
`src/ai/ai_resource.cpp:333-405,919-945`: depot/resource proximity uses a
15-tile search radius, assignment finds a depot before searching for its
resource, and a failed resource search creates an exploration request.

The current depot-mining-fix changeset keeps that knowledge boundary. An
enabled AI gold hauler re-evaluates known, live, reachable mines around the
completed dropoff only after a real delivery credits its cargo and the depot
wait completes. Human and non-manager workers retain the existing source order.
If an active known mine is farther than the source 15-tile radius from its
completed depot and no known reachable mine is inside that radius, the existing
single scout keeps its five-second cadence and flyer-first eligibility but
chooses its next owner-unexplored destination from the depot-centered search
ring. No resource is inspected or selected while unseen, no cargo is cancelled,
and no save field or depot build request was added. `AiNewDepotRequest` remains
a later optimization because the fix does not need a new construction request
or risk conflicting with scripted reservations.

`npm run verify:plan014-ai-scout` failed behaviorally before production changes
at PID `2404617`: all five haulers remained valid and cargo-safe, but the scout
chose generic tile `(102,35)`, mine 14 remained unknown, and a 100-gold delivery
resumed mine 3. The same public-seam fixture passed after the fix at PID
`2407511`, covering the exact 3,091-pixel commute, unseen-resource exclusion,
owner-buffer provenance, natural reveal, exactly-once 100-gold credit,
post-delivery retarget to mine 14, one-scout/no-duplicate behavior, non-manager
retention, and scout save/load determinism.

Final browser-free gates passed on Halla under root PIDs `2408558` and
`2409262`: Plan014 depot/scout, live manager/force/save safety, scheduler
(31.570 ms turn / 31.749 ms queued UI), source AI explores, runtime
determinism, fixed-demo random AI, save schema (50 fields), TypeScript, 1,182
Wargus assets, production build (765 modules), and `git diff --check`. No
browser, server, game page, deployment, push, save-schema mutation, or runtime
checkpoint mutation occurred. Full evidence is under
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan014-depot-mining-fix/`.
At that historical checkpoint, Plan014 remained **IN PROGRESS — NOT READY**
pending continuation from the unchanged P31 runtime lineage plus the
then-current Task 9 and independent READY gates.

## Historical pending Task 9 chain — superseded by user override

Before the final override, the required chain used the Codex in-app Browser and
the save/load protocol in
`plans/EXECUTION-GATES.md`. Each segment must stay below 30 seconds from page
load, load the prior accepted F12 slot, visibly click Run, reach one milestone
through normal controls, visibly Pause, F11-save, verify the slot, close, and
clean up. Record at least:

1. Visible selection of difficulty level 3 from the one-Peasant/no-Hall opening and player-built defense.
2. Enemy Hall unpaid travel, paid foundation/completion, and no duplicate or cancelled overcommit.
3. First and second production buildings completed.
4. Literal 1-, 4-, then 16-unit launches and player contact, with ids/ticks proving detachment and no reuse.
5. Visible difficulty down/up, one AI think after each change, and one real build or train duration.
6. A scout destination justified by the AI player's exploration buffer.
7. Update/render averages at progressed pressure milestones.

## Historical checkpoint note

The original A/B commits landed before this evidence file existed, so a
per-checkpoint READY-before-continuing record cannot be reconstructed. The new
whole-plan independent review was originally the remaining acceptance decision.
The final user override accepts the ordinary core-loop record without claiming
that the missing historical record was recovered.

## User-approved ordinary core-play acceptance — 2026-07-24

The user authorized one ordinary production-UI session, then one retry after the
first throwaway driver stopped before construction on an over-specific rate
expectation. After the retry stopped only because automation missed a moving
Peasant's current canvas hit point, the user ruled that this was not a
player-visible game blocker and explicitly accepted the retry together with the
preserved live milestones below. No third page or new verifier was run.

### Ordinary retry

- Fresh seed `ai-staged-pressure`, 1280x720, branch
  `codex/gameplay-roadmap`, source commit
  `380a0c7b5cfd1d6c51809c3e75f38a722518c6f2`.
- Opening tick 1: one selected Peasant, no Hall/Farm/Barracks, and
  `10,000 gold / 5,000 wood / 5,000 oil`.
- Normal controls placed the Town Hall at tick 158; it completed at tick 1701.
  The Farm was placed at tick 1797 and completed at tick 2405. The Barracks was
  placed at tick 2485 and visibly remained an active foundation at the final
  screenshot. The Hall trained `unit-peasant-24` at tick 2789.
- AI player 1 completed exactly one Hall and one Barracks by tick 2789, with no
  duplicate Hall/foundation/in-flight count observed. The match remained
  `playing` at the final diagnostic.
- Visible Pause held tick `197` exactly and Resume advanced to tick `213`.
  The final diagnostic reported last update/render `3.6/5.2 ms`, no page
  exception, and a responsive rendered HUD. Visual inspection of both
  screenshots found no black screen.
- Attempt 1 used port `56147`, page wall `20,626 ms`, and driver/server/browser
  roots `2477178/2477196/2477220`; it stopped before construction solely on the
  discarded rate expectation. Attempt 2 used port `56148`, page wall
  `92,056 ms`, and roots `2481817/2481835/2481859`. Both exact PID trees were
  stopped, both listeners were clear, and both reports record zero remaining
  PIDs.

### Preserved live milestones accepted with the retry

- `plan014-final-from-p13-result.json` records the player's Barracks
  `unit-human-barracks-34` completing at tick `6817`, a real 12-second Footman
  order at tick `6821`, and `unit-footman-35` completing at tick `7181`.
- The same file records AI force 1 launching `unit-grunt-25` with an `attack`
  order at tick `4411` and contacting the player's Town Hall at tick `4512`.
  It also records an owner-unexplored scout assignment at tick `6901` and that
  Peon returning to ordinary gold harvesting at tick `7198`.
- `plan014-final-from-p31-result.json` records five exact 100-gold delivery
  credits and five durable credits, reaching `goldMined=500`, with real
  `to-dropoff` orders targeting `unit-great-hall-18`. Its scout observations
  preserve owner-buffer provenance and a one-scout maximum.
- Both longer result files ended later on harness-level incomplete/close paths;
  the override cites only their immutable, structured live milestones and does
  not relabel either overall harness outcome as PASS.
- The current-head depot/scout report records the final `380a0c7` persistent
  explored-resource correction and green Plan014 AI runtime, source exploration,
  determinism, fixed-demo AI, save, TypeScript, 1,182-asset, and production-build
  gates. The tanker-rally re-review records **CODE READY FOR TASK 9**, and the
  scheduler implementation/follow-up records bounded UI yielding plus green
  runtime gates.

### Evidence hashes

```text
504492ee371853c5d2e06682688f75a9dabb6c806c7c16dad2c6a019a0ab7dc1  plan014-core-play/session-report.json
1c987cae4de1fdea3d13d5bba1fdf67bde507124c64ec58a55650585e36c5fe3  plan014-core-play/01-opening.png
91ab86597d4e45186944adbabb0b0d60369b7b28c4340efb948af478264bb0a4  plan014-core-play/failure.png
4d5f5c3c780bc40be6fe8a300bd020d1a23e556fd666e0f9212e4b7c6bdabe54  plan014-core-play/attempt-1-driver-rate-stop/session-report.json
3cfaab50f0ee20f7fbe4b1f9de549b2151ba15708506461e0e62f242c0725324  plan014-final-from-p13-result.json
8134ef8617e6401e8f9b689ded90e5c7f6aafa0eb15caa15bc8f05c1586eca72  plan014-final-from-p31-result.json
43d0b32d0492f0dde1150511d7dd0669f073cb965d75288d61435dad1892ffdd  plan014-depot-mining-fix/implementation-report.md
9d3da769bd6bd713ee5797ba878317ffcf2027ff1f6ba309f6ea4ed678dc5abb  plan014-scheduler-fix/implementation-report.md
f801e13f280b87d9c4dc735cafb98269b0b3ea966c070f6272d0336e36c6a088  plan014-tanker-rally-rereview-report.md
```

### Acceptance boundary

This READY decision is the user's explicit product acceptance of the ordinary
core loop. It does not claim exhaustive Gate A, a new M07 replay, the full
difficulty sequence, a same-session second Barracks, or live 4/16 waves. Those
items are superseded by the override; focused source/interpreter coverage remains
the regression guard. No Plan016/017 work, deployment, push, fixture mutation,
cheat, hidden world/order write, or save replay was performed in the ordinary
retry.

## Review decision

- Decision: **READY — DONE**
- Reviewer/acceptor: **User, explicit 2026-07-24 acceptance override**
- Reason: the ordinary retry showed a healthy fresh opening and responsive base
  loop through Hall/Farm/Barracks construction and a second worker; preserved
  live evidence supplies Barracks/Footman completion, real economy credits,
  owner-buffer scouting, and AI launch/contact; reviewed current-head fixes are
  green. Exhaustive Task 9/Gate A proof is explicitly waived and not claimed.
