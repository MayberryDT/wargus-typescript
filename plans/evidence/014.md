# Plan 014 Evidence

- Branch: `codex/gameplay-roadmap`
- Planned baseline: `6af2eebb71732a76ae36dfeb56a570c854634ab0`
- Plan execution base: `c94032281d0ed24a5f76e3f3fcd4847464e24249`
- 014-A script execution: `58fed9ddd5bc21932c0fa9bd2b06e8e1b7e86b2d`
- 014-B timing boundary: `6601356ee896325c0ca3c071f0c87e2979b9f68e`
- 014-C, minimap performance, and M07 evidence implementation head: `bfad8d6492ce63bb0172bd6578d1a8d6db232538`
- Evidence/checklist head: pending this closeout commit
- Date/time zone: `2026-07-23T20:36:13-06:00` (America/Chicago)
- Execution host/worktree: `halla` / `/home/halla/workspaces/Wargus-TypeScript-roadmap`
- Implementation diff: 10 files, 1,197 insertions, 360 deletions in `bfad8d6`

## Dependency and source authority

Plans 011, 012, 013, and 015 are `DONE` and their evidence packets record
independent READY decisions. Plan 014 follows the source-AI contracts recorded
in `plans/ORIGINAL-WARGUS-SOURCE.md`: installed Wargus commit
`409a36f7da7b6a162e3f465d98619db9a9d77cbf` and installed Stratagus commit
`b3b4babe8eb8f908ac9cb25da9c0fd83c67957c5`. Halla verification used the
mirrors at `/home/halla/workspaces/Wargus-TypeScript-artifacts/original-wargus`
and `/home/halla/workspaces/Wargus-TypeScript-artifacts/original-stratagus`.

The implementation preserves the installed producer graph: Barracks produce
Ballista/Catapult, Inventor produces Flying Machine/Dwarves, and Alchemist
produces Zeppelin/Goblin Sappers. It also preserves the source distinction
between additive `AiNeed`, absolute `AiSet`, and tier/tower `upgrade-to`, the
fixed once-per-second AI cadence, immediate detached force launches, and
source percentages normalized to factors at the runtime boundary.

## Runtime acceptance

All accepted runs used one 1280x720 tab, seed `ai-staged-pressure`, Node 22,
low scheduler priority, a strict dedicated loopback port, the unchanged 20 ms
update and 24 ms render ceilings, and exact verifier-owned PID-tree cleanup.

| Scenario | Port | Result | Wall / telemetry | Evidence |
|---|---:|---|---|---|
| Optimized steady profile | 55732 | PASS | page 7.885 s; command 10.39 s; update 4.773821 ms; render 19.079281 ms | Opening contract passed at both samples; one tab; exact cleanup. |
| M08/M09 default Plan 014 | 55737 | PASS | wall 10.7 s; update 4.54 ms; render settled at or below 24 ms | Launches `1 -> 4 -> 16`, 21 unique launched ids, two desired Barracks, factors `0.75/1/1/1.2/1.5`, reset factor `1`, positive sleep barrier, AI-owned exploration, save compatibility, manifest/canvas/cache/opening checks all passed. |
| M01 replay | 55738 | PASS | wall 10.1 s; update 4.32 ms; render settled at or below 24 ms | Unpaid `to-site`, paid arrival, hidden builder, cancel removal/release, and 75% refund all passed. |
| M04 replay | 55739 | PASS | wall 10.0 s; update 4.50 ms; render settled at or below 24 ms | Five-unit formation completed `5/5` with the source-relative destinations and no order-loss regression. |
| M07 first replay | 55740 | FAIL, corrected | failure returned below the 30 s outer limit; failure path did not print averages | Automatic return reached `0px`, but the fixture omitted independent Hold evidence. Exact cleanup passed. |
| M07 corrected replay | 55741 | PASS | wall 9.8 s; update 4.43 ms; render settled at or below 24 ms | Automatic return `0px`; Hold Position movement `0px` with null target; explicit attack `autoReturn: null`. |

The accepted default run reported a 1280x720 canvas with 386 sampled colors.
It preserved the fixed-demo opening: one selected and owned Peasant, 19 total
units, no Hall/Farm/Keep/Castle, 10,000 gold / 5,000 wood / 5,000 oil, speed 1,
a successful `/wargus/manifest.json` fetch, nonblank pixels, viewport-bounded
terrain, and one browser tab.

### M08 force and opening contract

- The installed zero-cycle initial sleep advances, while a separate positive
  sleep blocks, advances once at its deadline, and clears that deadline.
- Detached launches occur in literal sizes `1`, `4`, then `16`; all 21 ids are
  unique and no launched id satisfies a later scripted slot.
- Successful launch records have non-negative ticks, the next declaration
  advances to its wait barrier, and source-script players avoid the legacy
  mutable wave selector.
- Additive and absolute build requests settle at two desired Barracks in the
  staged fixture; pending construction costs are reserved and the opening Hall
  is not orphaned or double-paid.
- The installed four Blacksmith research requests precede the first force and
  the Barracks/Inventor/Alchemist producer graph is unchanged.

### M09 timing and knowledge contract

- Difficulties 1-5 yield build/action factors
  `0.75, 1, 1, 1.2, 1.5`; returning to level 3 resets the factor to `1`.
- AI management remains once per simulated second. Difficulty scales explicit
  sleeps and speed factors without multiplying the manager cadence or making
  the positive-sleep fixture complete instantly.
- AI exploration grows the AI player's buffer while retaining the visibility
  player's separate alias, and target selection distinguishes an unexplored AI
  tile from the same tile already explored by the human buffer.
- New saves preserve each exact map-sized buffer and cadence ticks. Legacy
  saves copy `exploredTiles` only into `visibilityPlayer`, leave the AI buffer
  zeroed, and rebind the rendering alias correctly.

## Minimap performance gate

The initial profile identified full 128x128 minimap Graphics reconstruction as
the render-budget cause. The accepted cache keeps one bounded canvas texture,
one sprite, one dynamic overlay, and one separate hit target per HUD layer;
terrain classification is invalidation-keyed, fog is composited in exact
row-major terrain-then-fog order, pointer handlers remain single-instance, and
owner teardown destroys the TextureSource exactly once.

| Metric | Baseline port 55731 | Optimized port 55732 | Delta |
|---|---:|---:|---:|
| Page wall | 8.531 s | 7.885 s | -0.646 s |
| Average update | 6.97 ms | 4.773821 ms | -2.196179 ms (-31.5%) |
| Average render | 35.69 ms | 19.079281 ms | -16.610719 ms (-46.5%) |
| `drawMinimap` total | 175.17 ms | 103.38 ms | -71.79 ms (-41.0%) |
| Garbage collection | 89.31 ms | 3.45 ms | -85.86 ms (-96.1%) |

Independent minimap review v4 at
`/home/halla/workspaces/Wargus-TypeScript-artifacts/minimap-render-cache-review-v4-report.md`
is READY with 0 Critical, 0 Important, and 0 Minor findings.

## Diagnostic lineage and cleanup

These earlier bounded runs explain the corrections without being used as final
acceptance evidence:

| Port | Result |
|---:|---|
| 54314 (two attempts) | One-tab default smoke timed out at the 25 s page watchdog; exact cleanup passed after each attempt. |
| 55473 | Diagnostic reached screenshot at 7.923 s, then the AI script fixture did not return by 25 s; exact cleanup passed. |
| 55519 | Minimum clone correction reached screenshot at 7.254 s but failed at the same 25 s boundary; the ineffective correction was removed and exact cleanup passed. |
| 55621 | Command wall 13.792 s; stale resource-field assertion and render 63.0354 ms failed; exact cleanup passed. |
| 55625 | Command wall 11.256 s; corrected resource lookup passed but render did not settle to 24 ms in the bounded wait; the correction cycle was preserved for later profiling and cleanup passed. |
| 55731 | Baseline CPU profile reproduced render 35.69 ms; page 8.531 s, command 12.38 s; exact cleanup passed. |
| 55733 | Post-cache default failed only the render ceiling at 28.106819 ms; command 8.68 s; exact cleanup passed. |
| 55734-55736 | Reserved by the stopped ordered attempt and never launched. |
| 55737-55741 | Final default/M01/M04/M07 sequence above; every launched port was exact-cleaned. |

Final inspections found every owned port clear and no verifier-created Vite,
Playwright, or Chromium process. No pre-existing Halla service, automation,
container, app, process, or port owner was stopped or reconfigured.

## Static closeout gates

All commands ran sequentially on Halla under Node `v22.22.2` and `nice -n 10`:

- `./node_modules/.bin/tsc --noEmit` — PASS.
- `npm run verify:fixed-demo-random-ai` — PASS.
- `WARGUS_ORIGINAL_SOURCE=/home/halla/workspaces/Wargus-TypeScript-artifacts/original-wargus npm run verify:source-ai-difficulty` — PASS.
- `npm run verify:source-ai-forces` — PASS (74 plans across 40 campaign maps).
- `WARGUS_ORIGINAL_STRATAGUS_SOURCE=/home/halla/workspaces/Wargus-TypeScript-artifacts/original-stratagus npm run verify:source-ai-explores` — PASS.
- `npm run verify:minimap-render-cache` — PASS.
- `npm run verify:playtest-telemetry` — PASS.
- `npm run verify:runtime-determinism` — PASS; no random or wall-clock APIs under `src/**/*.ts`.
- `npm run verify:save-schema` — PASS; 50 persisted `WorldState` fields checked.
- `npm run verify:wargus-assets` — PASS; 1,182 files checked.
- `npm run build` — PASS; asset preflight passed and Vite transformed 764 modules.
- `node --check` for the browser-runtime, source-exploration,
  source-force, and minimap-cache verifiers — PASS.
- `git diff --check` — PASS before the implementation commit.

## Deterministic and bounded state

- `sourceScriptForces`, detached launch evidence, build desires, and active ids
  are normalized and bounded; launch history cannot grow past the script's
  finite attack instructions because EOF does not rewind.
- `exploredTilesByPlayer` contains exact map-sized byte buffers, AI visibility
  refresh is capped at once per `tickRate`, and new scout assignment is capped
  at once per five simulated seconds per AI.
- The minimap cache is one `WeakMap` entry per HUD layer, reuses its canvas,
  texture, sprite, hit target, and two pointer handlers, and provides
  deterministic idempotent disposal.
- Runtime determinism and save-schema gates pass after the final production
  changes.

## Residual risk

- The implementation and bounded browser scenarios are complete, but a
  separate whole-plan reviewer has not yet issued the required READY decision.
- Task 9's long-form manual staged-pressure play from one Peasant, including
  changing difficulty through the visible menu, was not separately recorded;
  the accepted browser evidence is deterministic and fixture-assisted. The
  reviewer must decide whether to require that additional manual session.
- `verify:source-map-minimap-layout` still depends on a workstation-only
  manifest `dataRoot` absent on Halla. The focused minimap gate, asset gate,
  production build, live cache assertions, and optimized profile all pass, but
  the source-layout verifier was not part of this closeout run.

## Review decision

- Decision: **PENDING — NOT YET REVIEWED**
- Reviewer: pending separate whole-plan review
- Reason: implementation, static gates, performance budgets, and M01/M04/M07-M09 evidence are recorded; independent review and the manual-session disposition remain outstanding.
