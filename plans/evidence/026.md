# Plan 026 acceptance evidence

Status: DONE-VERIFIED. The reviewed implementation, coordinator-owned integration, retained-artifact proof, controller gate, and qualifying hardware-renderer proof are complete. This foundation plan intentionally did not run a performance matrix.

## Environment and entry preflight

- Host/worktree/branch: `halla`, `/home/halla/workspaces/t3/Wargus-TypeScript/.worktrees/plan-026`, `wave0/plan-026` at entry SHA `170cc7dc5df1e1b4f15de2efcd2c70a72a268479`.
- Fresh group context: `sg video -c 'sg render -c "test -r /dev/dri/card1 && test -r /dev/dri/renderD128"'` exited 0 using the required nested video/render group context. `halla` is listed in both `video` and `render`.
- DRM devices: `/dev/dri/card1` is `crw-rw---- root:video`; `/dev/dri/renderD128` is `crw-rw---- root:render`.
- Pre-mutation host metrics: MemAvailable `9855381504` bytes, swap used `315392` bytes, workspace free `351105359872` bytes, load `1.22 0.76 0.58`; each start threshold passed. The controller resource helper later sampled MemAvailable `9993269248` bytes, swap used `315392` bytes, workspace free `351100903424` bytes, load `0.64 0.84 0.70`, CPU summary, and GPU `unavailable`.
- Listener inventory was inspected before every fixture spawn. Pre-existing listeners include Node PID `1566951` on `4173`, `3773`, `42853`, and `9229`; this plan neither owned nor touched them. No conflicting Wargus benchmark process was found.
- The baseline fixed-port inventory contained the planned static owners, including the 5203/9230 collision. The branch-local migration scan now finds no `const PORT`, `const DEBUG_PORT`, detached process groups, process-group kill, or obsolete dedicated cleanup call in the migrated verifier set.
- Artifact exclusion baseline: `git check-ignore -v .artifacts/performance/026/probe/file.json` exited 1 and printed no rule, as required before coordinator integration.

## Controller implementation and focused verification

Implemented `scripts/lib/browser-execution-controller.mjs` and `scripts/verify-browser-execution-controller.mjs`. The controller reserves inspected server/debug ports, tracks an allocation ledger, records roots and descendants, performs descendant-first TERM then exact-survivor KILL cleanup, rejects software renderers, uses a 120000ms no-progress readiness watchdog, has no valid-capture tab-duration ceiling, samples host resources, supports owned-only safety abort, and validates the retained artifact root before capture.

RED command and output:

```text
sg video -c 'sg render -c "node scripts/verify-browser-execution-controller.mjs"'
ERR_MODULE_NOT_FOUND: scripts/lib/browser-execution-controller.mjs
```

GREEN command and output:

```text
sg video -c 'sg render -c "node scripts/verify-browser-execution-controller.mjs"'
Allocation fixture ledger: [{"serverPort":41305,"debugPort":41306,"allocatedAt":1785296576839,"requestedServerPort":null},{"serverPort":41307,"debugPort":41308,"allocatedAt":1785296576839,"requestedServerPort":null}].
Controller fixture cleanup: root=1980457; terminated=1980471,1980457; residual=none; sentinel=1980458 survived.
Safety fixture cleanup: root=1980522; terminated=1980522; residual=none; sentinel=1980523 survived.
Browser execution controller verified: occupied-port refusal, unique allocations, exact descendant cleanup, sentinel survival, renderer qualification, readiness watchdog, and unlimited valid capture lifecycle.
```

The post-command `ps` check found no fixture `setInterval` process. Fixture ports were reservations only and were clear after cleanup. No browser server, Chrome, or benchmark process was started for this checkpoint.

The verifier covers occupied-port refusal before spawn, distinct ledgered allocations, descendant cleanup, unrelated-sentinel survival, SwiftShader rejection, hardware metadata acceptance fixture, 120-second readiness stall, unlimited valid capture lifecycle, and owned-only resource safety abort with a recorded cleanup result. The renderer result is fixture-level only; no hardware renderer has been accepted for frame-budget evidence.

## Browser verifier migration

Migrated the shared smoke harness, map-load, playable, harvest, spell, fixed-demo input, train, demo-victory, command-card, modern-HUD, runtime-smoke, and combat-session lifecycle/port surfaces. Each now allocates controller ports or uses the explicit requested runtime/combat candidate, records owned processes, and delegates cleanup to the controller. `WARGUS_BROWSER_RUNTIME_PORT` has no fallback default.

`node scripts/verify-browser-native-viewport.mjs` passed. All migrated files and controller verifier passed `node --check`; the scoped lifecycle scan and `git diff --check` passed.

## Independent review and coordinator integration

- Branch implementation commits: `45b0f63e6cec59dd63359311489934b798e15463`, `51a828bf719bd2387db2f3f47623aadef6798c32`, and `7a574ae64e75a77126b7c4bf46143cd02036c2a0`.
- Independent review found seven Important and two Minor issues. The two required fix rounds closed every Important finding; the final rereview approved the production `runCapture` lifecycle with advancing RAF and protocol-owned stop behavior. Deferred cleanup observations are recorded in the Plan 026 SDD ledger and are not acceptance blockers.
- Coordinator equivalents are `eb0cb0e`, `28c886d`, and `02051ad`. Commit `d99a24a37d273f98b37c78e2ea4f750513b8abfe` added the package-script gate and root `/.artifacts/` ignore rule.
- `git check-ignore -q .artifacts/performance/026/probe/file.json` passed from the retained checkout after integration.

## Retained artifact proof

- Retained checkout: `/home/halla/workspaces/t3/Wargus-TypeScript-retained-artifacts`, detached at `d99a24a37d273f98b37c78e2ea4f750513b8abfe`.
- Preservation owner: Wave 0 coordinator.
- Retained root: `/home/halla/workspaces/t3/Wargus-TypeScript-retained-artifacts/.artifacts`; it was writable, outside the disposable worktree realpath, ignored by Git, and had more than 350 GiB free.
- A disposable fixture worktree at `/tmp/wargus-plan026-disposable-20260729T040304Z` wrote the artifact through the real controller helper and was then removed with `git worktree remove`.
- The artifact survived removal at logical path `.artifacts/performance/026/d99a24a37d273f98b37c78e2ea4f750513b8abfe/20260729T040304Z/retention-proof.json`, physical path `/home/halla/workspaces/t3/Wargus-TypeScript-retained-artifacts/.artifacts/performance/026/d99a24a37d273f98b37c78e2ea4f750513b8abfe/20260729T040304Z/retention-proof.json`, SHA-256 `0f21abc4a171669ca3580e89c45a062228ca173b3e391e2139705beb2bf12f6a`.

## Integrated controller gate

The package gate ran in a fresh video/render group context with the retained workspace/root variables:

```text
npm run verify:browser-execution-controller
Allocation fixture ledger: server/debug ports 35449/35450 and 35451/35452.
Controller fixture cleanup: root=2008165; descendant=2008179; residual=none; sentinel survived.
Safety fixture cleanup: root=2008250; residual=none; sentinel survived.
Browser execution controller verified.
```

All allocated fixture ports were clear after cleanup.

## Hardware renderer qualification

The T3 in-app preview was attempted first. The app loaded, but preview automation was background-throttled: RAF and synchronous evaluation timed out. The approved headless-Chromium fallback was then used.

An initial EGL launch reported SwiftShader and was rejected as required. The successful retry used Google Chrome `150.0.7871.124` with ANGLE Vulkan and the host AMD GPU:

```text
renderer: ANGLE (AMD, Vulkan 1.4.318 (AMD Radeon Graphics (RADV RENOIR) (0x0000164C)), radv)
vendor: Google Inc. (AMD)
WebGL: 2
viewport: 1280x720 at DPR 1
document.hasFocus(): true
visibilityState: visible
RAF timestamps: 393.2, 443.2, 459.9, 493.1, 509.8
executable: /usr/bin/google-chrome
GPU: AMD Lucienne 1002:164c
kernel driver: amdgpu
```

RAF advanced and every renderer qualification check passed.

## Cleanup and final host state

- Every Vite, Chrome, controller-fixture, and disposable-worktree process started for acceptance was tracked by exact PID and cleaned by exact ownership. The successful Chrome root was PID `2015592`; no descendant remained.
- Port `43127` and all dynamically allocated controller ports were clear after cleanup. Pre-existing Halla listeners and processes were untouched.
- Post-run host metrics: MemAvailable about 8.8 GiB, swap used about 308 KiB, workspace free about 327 GiB, load averages `0.53 0.59 0.62`.

Verdict: Plan 026 is accepted as `DONE-VERIFIED`. No performance matrix was run or claimed.
