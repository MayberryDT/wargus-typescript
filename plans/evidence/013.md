# Plan 013 Evidence

- Final implementation commit: `4bcd6c9`
- Branch: `codex/gameplay-roadmap`
- Date/time zone: `2026-07-23` (America/Chicago)
- Source authority: installed Stratagus source at `b3b4babe8eb8f908ac9cb25da9c0fd83c67957c5`
- Execution host: Halla, isolated checkout `/home/halla/workspaces/Wargus-TypeScript-roadmap`

## Implementation checkpoints

- `c3468e9` — attack-range-valid routing, automatic current-reaction chase/return, explicit `autoReturn: null`, dead-target cleanup, and save normalization/soft references.
- `15c645c` — committed projectile validity separated from trajectory and acquisition, fixed-ground nonzero-range impacts, fog-independent area damage, and installed-source ownership rules.
- `4ceab5f` — local plus visible-enemy combat audio, with hidden and coordinate-less enemy events suppressed as the port's anti-information-leak policy.
- `4bcd6c9` — deterministic M05–M07 fixtures and a one-tab Playwright verifier with exact process-tree cleanup.

## Player-visible scenarios

| Scenario | Result | Evidence |
|---|---|---|
| M02 replay | READY | Temporary congestion retained the exact Move for 12 blocked ticks, completed at tick 88 on tile `(6,1)`, and reported no empty live path or overlap. Maximum retry update was `5.5ms`. |
| M04 replay | READY | Five Footmen completed the preserved source formation at `(9,7)`, `(10,7)`, `(11,7)`, `(10,6)`, and `(10,8)` with zero drops, empty-path ticks, or overlaps. Semantic replay matched; maximum update was `0.8ms`. |
| M05 unreachable aggro | READY | Attack-move acquired the isolated enemy, rejected its statically unreachable attack-range route, resumed the stored destination, and completed instead of freezing. |
| M06 committed damage | READY | A zero-range point-to-point projectile dealt `7` damage once after its live compatible target moved and visibility was removed. A nonzero-range siege control dealt `0` to the moved stored target and `6` to the compatible unit at the immutable ground point. |
| M06 area ownership | READY | With zero visible tiles, default Blizzard-style area damage produced deltas `caster=0`, `own=9`, `allied=9`, `enemy=9`, `neutral=9`. Demolish produced `caster=40`, `own=400`, `allied=400`, `enemy=400`, `neutral=400`, matching installed source ownership behavior. |
| M07 automatic response | READY | The idle Footman captured its exact origin, chased from outside melee range, damaged the target, and returned to `29.9px` in M05 and `0px` in the focused M07 timeline. Hold Position moved `0px`; an explicit attack persisted `autoReturn: null`. |
| Visible/hidden audio | READY | Visible enemy playback incremented once; hidden and coordinate-less enemy playback remained zero. Local events remained audible. |

## Save/load contract

The save schema gate confirms old attacks without `autoReturn` normalize to
`null`, valid finite in-bounds automatic returns survive roundtrip, and missing
or temporarily hidden soft targets remain long enough for the attack step to
restore the saved return order.

## Browser acceptance and resource discipline

- One Playwright-controlled Halla tab at 1280x720 — PASS.
- Combat fixture session completed under the 25-second internal limit and printed all M05–M07 results.
- M02/M04 replay completed under the same 25-second limit in a separate one-tab probe.
- Each run used a unique loopback port and killed only the exact Vite/Chromium process tree it started. No local game/browser workload and no Halla automation, app, service, or container was touched.
- Two earlier handmade-CDP attempts reached the 30-second ceiling before returning results. Both were stopped and their exact verifier-owned PID trees were removed; the transport was then replaced with the proven Playwright runtime.

## Verification

- `./node_modules/.bin/tsc --noEmit` — PASS
- `npm run verify:browser-combat-session` — PASS
- `npm run verify:source-attack-action` — PASS
- `npm run verify:source-fov-fog` — PASS
- `npm run verify:source-event-audio-pan` — PASS
- `npm run verify:save-schema` — PASS
- `npm run verify:runtime-determinism` — PASS
- `git diff --check` — PASS

## Review decision

- READY
- Reviewer: `/root/plan013_review`
- Scope: read-only review of the four Plan 013 implementation commits; no browser, server, build, or test process was launched by the reviewer.
- Findings: none.

Review decision: READY
