# Plan 016 Evidence

> **Active historical summary — `ACCEPTED-WAIVER`:** The user accepted the
> ordinary integrated session and passing browser-free gates. Exhaustive
> M01/M04/M10–M12 replay, forced completion-supply/no-egress fixtures, index-2
> cancellation, the second viewport, exact pointer-pan delta, unavailable
> original-source gates, and unrun browser gates remain waived or not run as
> recorded below; they are not passed claims.

- Branch: `codex/gameplay-roadmap`
- Implementation baseline: `f8a1c7722074c775177fd7f3504d520402dee475`
- Integrated acceptance baseline: `a17bfa7380cbbb256c6f951b86952726f3111259`
- Date/time zone: 2026-07-24, America/Chicago
- Execution host: `halla`
- Decision: **DONE — READY under the final ordinary-play user override**

## Implementation

The batch normalizes imported icon tileset ids, removes authoritative queued
food and queued unit-limit reservation, persists production block reasons at a
valid zero-second paid head, preserves paid queues across save normalization,
searches outward across the whole map for producer egress, renders six indexed
fixed-HUD queue targets with overflow, corrects supply/range/hint/status text,
persists setup-derived teams, applies source building/mobile mixing during
additive rectangle selection, and recomputes pointer world/drag coordinates
immediately after camera movement.

The one-Peasant/no-Hall/high-resource opening, 127-order queue limit, costs,
durations, dependencies, damage, AI, pacing, manifest, and asset pack remain
unchanged.

## Browser-free gate results

- `verify:icons`: PASS — 532 references.
- `verify:save-schema`: PASS — 50 persisted `WorldState` fields.
- `verify:runtime-determinism`: PASS.
- standalone `tsc --noEmit`: PASS.
- `verify:wargus-assets`: PASS — 1,182 files.
- production `npm run build`: PASS — 765 modules transformed.
- `git diff --check`: PASS.
- `verify:source-selection-mixing`, `verify:source-status-line-tooltips`,
  `verify:source-upgrade-to-action`, and `verify:source-info-panel-layout` were
  invoked once but unavailable on Halla because their scripts hardcode missing
  `/home/tyler/...` original-source paths. Per user direction, verifier
  infrastructure was not expanded.
- `verify:modern-hud-layout` and all browser/playable gates were intentionally
  not run because they start a server/browser.

The build lifecycle invoked the asset preflight internally after the explicit
asset gate, so the same 1,182-file check appeared twice even though the batch
requested it explicitly only once.

Exact commands, PIDs, timestamps, and output are in
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan016-evidence/gates.log`.

## Final integrated ordinary play

The accepted session is
`/home/halla/workspaces/Wargus-TypeScript-artifacts/final-integrated-play/attempt-7-ordinary-integrated-2026-07-25T00-02-54Z/`.
It used one fresh 1280×720 page on port `56281`, seed
`plan017-b-target-21`, and commit `a17bfa7`; page wall time was `143,759 ms`.
Only normal pointer/keyboard controls changed game state. Smoke/debug reads were
limited to current location and observation, and the run used no fixture,
cheat, direct world/order write, hidden action hook, replay, or second page.

- Before launch, the authoritative placement predicate produced 16 visible,
  HUD-free, terrain/occupancy-valid and Peasant-reachable 4×4 Town Hall sites,
  58 Farm sites, and 40 Barracks sites. Packet SHA-256:
  `b43e5376048b62d00c95b90df0fbf36598db44dbe53071e88410dadebf8ff2be`.
- The first dynamic Town Hall candidate, tile `(117,114)`, accepted at tick
  `233`; Farm `(121,115)` and Barracks `(121,112)` also accepted on their first
  visible attempts. All three completed normally.
- The opening screenshot shows real icons, `Food 1/0 · Build...`, one selected
  Peasant, and the normal command card. Later read-only HUD snapshots reported
  `Needs 1 Food`, `Requires Barracks`, `Requires Elven Lumber Mill`, and the
  multi-prerequisite Ballista/Knight reasons.
- Two paid Peasants were simultaneously visible in the Town Hall queue while
  food remained `1/5`; the first completed as `unit-peasant-24`. A Footman was
  visibly queued and completed as `unit-footman-28`.
- The extra Peasant harvested 100 gold and returned it through the visible
  Return command, moving gold `6800 -> 6900`. A Footman plus Peasant additive
  selection retained both mobile types and accepted a visible Move order.
- Pause held tick `4700`; Resume advanced. F11 saved paused tick `5019`, F12
  restored tick `5019` in the same page, and Run resumed from the loaded state.
- Opening, construction, queue, Footman, pointer-preview, and final screenshots
  all passed non-black pixel checks. There were no page exceptions. Two generic
  404 console messages and one aborted Orc briefing audio request were recorded
  without a player-visible failure or icon-atlas warning.
- Final average update/render were `7.007/13.677 ms`, within the shared
  `20/24 ms` budgets. The final page remained `playing`, unpaused, and
  responsive.

The user explicitly accepted this ordinary integrated session plus the recorded
browser-free gates as the Plan 016 release boundary. Exhaustive M01/M04/M10
replay, forced completion-supply/no-egress fixtures, index-2 cancellation, the
second viewport, and an exact stationary-pointer camera delta were waived and
are not retroactively claimed. The pointer preview rendered non-black, but its
automation did not capture a camera delta; that is retained as tool limitation,
not relabelled as an M12 proof or a player-visible defect.

## Residual risk

No unresolved release blocker remains under the user override. Completion
block/no-egress edge cases, index-specific queue cancellation, the second
viewport, and exact stationary-pointer camera coordinates retain static rather
than fresh ordinary-play coverage and should be replayed if those seams change.

## Review decision

- Decision: **READY — DONE**
- Reviewer: user-directed final ordinary-play override, verified by Codex on Halla
- Reason: the clean one-page session proved the ordinary HUD, build, queue,
  production, economy, selection, feedback, and save/load loop; preserved
  static evidence covers the explicitly waived exhaustive cases.
