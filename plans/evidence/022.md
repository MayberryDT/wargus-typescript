# Plan 022 Evidence — Retained World Display Objects

## Status

**IMPLEMENTED AND REVIEWED — EXACT VISUAL PARITY PASSED; INCREMENTAL
PERFORMANCE ACCEPTANCE DEFERRED TO PLAN 024 RECOVERY.**

Plan 022 is implemented on `perf/plan-022` at
`2c471915a04019a6618f3e958097a4162741534d`.

The renderer now retains independently owned Pixi records for units,
last-seen buildings, corpses, projectiles, and spell effects. Stable IDs retain
record identity; explicit shape keys bound record recreation; child slots are
reused; painter order remains flattened and stable; dormant records are
bounded; world replacement, save-load replacement, split-view closure, and
page teardown dispose their exact cache owners. Plan 018's existing
create/destroy tracker is extended under `plan022.worldRenderCache`; no second
lifecycle telemetry system was introduced.

## TDD and focused verification

The first counter test failed with `TypeError` because the accepted tracker had
no `plan022` namespace. The same behavior assertion passed after the additive
namespace and renderer tags were implemented. A second failing static contract
required primary and split-view cache disposal at all three world lifecycle
boundaries; it passed after the coordinator disposal function and exact calls
were added.

Final focused gates at the implementation SHA:

| Command | Result |
|---|---|
| `node scripts/verify-world-render-cache.mjs` | PASS — identity, ownership, order, bounds, pooling, counters, and disposal |
| `npm run verify:performance-metrics` | PASS |
| `npm run verify:playtest-telemetry` | PASS |
| `node scripts/verify-render-preparation.mjs` | PASS |
| `./node_modules/.bin/tsc --noEmit` | PASS |
| `npm run verify:runtime-determinism` | PASS |
| `npm run verify:wargus-assets` | PASS — 1,182 files |
| `npm run build` | PASS |
| `git diff --check` | PASS |
| `npm run verify:browser-runtime-smoke` under Halla video/render groups | PASS |
| `npm run verify:browser-native-viewport` with pinned source root | PASS, including fullscreen and viewport-mode checks |

The full playable verifier reproduced an accepted-base synchronous
defensive-auto-attack pathfinding burst on `human-exp/levelx10h.smp.gz`.
Targeted accepted-base and Plan 022 runs produced the same stack through
`sourceAttackTargetPathResult`, `findPathResult`, `searchReachable`, and
`footprintSearchCost`. Maps 1–9 and 11 passed; map 12 reproduced the known X12
first-tick pathfinding burst. These are preserved as Plan 024 recovery inputs;
the unfiltered playable gate must be rerun after Plan 024.

## Independent rereview corrections

The standards axis found no hard violations and two non-blocking naming/duplication
judgements. The spec axis found that the first integration checkpoint lacked
required reused/active/dormant/pooled telemetry and the five-minute churn
proof. Commit `2c471915a04019a6618f3e958097a4162741534d` corrected both
findings.

The existing tracker now aggregates independent cache owners and reports
per-kind `trackedCreated`, `reused`, `trackedDestroyed`, current
`active`/`dormant`/`pooled`, and their capture high-water marks. Exact owner
disposal removes only that owner and all-owner disposal returns current values
to zero while retaining window high-water evidence. The focused verifier
executes 18,000 deterministic frames (five simulated minutes at 60 FPS) for
each kind. Observed high-water values were: unit `1/256/0`, last-seen building
`1/128/0`, corpse `1/64/0`, projectile `1/64/1`, and spell effect
`1/64/1` for active/dormant/pooled respectively. All hard caps passed.

## Exact visual parity

Retained packet:
`.artifacts/performance/WAVE-2-INTEGRATION/2c471915a04019a6618f3e958097a4162741534d/20260730T185259Z/visual-parity/`.

The `combat-100` fixed state at tick 0, 1280×720, DPR 1 matched the pinned
pre-optimization base exactly: identical PNG and raw RGBA hashes, zero changed
pixels, zero maximum channel delta, and 10,089 RGB colors. The 10-member
visual manifest SHA-256 is
`53500605e09ca53113aee3a4195a6b07b7ff9fb20a7d1cc471ca7c8353977440`.

## Determinism and performance packet

Retained packet:
`.artifacts/performance/022/31d7136489d0b97896490a3123ca4dd3b1606c91/20260730T182358247Z/`.

The fixed-tick proof passed all five canonical profiles at the exact 600-tick
offset with two identical runs per profile. The targeted cache verifier passed
from the capture SHA. All 21 trials were valid and qualified with zero
replacement attempts: seven each for rows 3 (`army-100`), 4 (`army-200`), and
6 (`combat-100`). Every captured Plan 022 per-kind create/destroy/live-delta
counter remained zero, proving retained world records did not churn during the
measurement windows.

Rows 3 and 4 passed incremental acceptance. Row 6 retained the same 50.0 ms
median-trial and pooled frame p95 as the accepted baseline, but one trial
recorded 0.0147667 seconds of scheduler dropped time. That introduced the sole
new budget-failure key, so the exact normalized incremental verdict is
`NOT READY`; no valid trial was discarded and no threshold was weakened.
Plan 024 owns the already-reproduced synchronous pathfinding/scheduler bursts,
so this packet is retained as its pre-correction evidence and Plan 022's exact
21-trial matrix will be rerun after Plan 024.

The packet's 31-member checksum manifest independently matched every retained
member; the `sha256.json` SHA-256 is
`d8b10fed1b5b1749217353c8ef15a21f677d4ad4af655fa155b0171fc96eeeab`.
Finalization reported no errors, the exact owned PIDs were terminated, no
owned ports remained open, and the capture lock was released.
