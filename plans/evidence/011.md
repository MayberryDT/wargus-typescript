# Plan 011 Evidence

- Commit: `a93ef25`
- Branch: `codex/gameplay-roadmap`
- Date/time zone: `2026-07-10T10:43:59-06:00` (America/Denver)
- In-scope diff: 12 files, 800 insertions, 98 deletions from `1271534..a93ef25`

## Baseline reproduction

| Scenario | Seed | Before behavior | Evidence |
|---|---|---|---|
| M01 construction placement/retask | `construction-lifecycle` | Placement immediately deducted 1200 gold/800 wood, incremented unit serial/count, and created a paid 10%-HP Hall before the Peasant reached the site. | Browser RED snapshot: resources `100000/100000 -> 98800/99200`, units `21 -> 22`, serial `34 -> 35`, one foundation. |

## After behavior

| Scenario | Seed | Result | Tick/wall time | Evidence |
|---|---|---|---|---|
| M01 unpaid travel and six retasks | `construction-lifecycle` | Move, Stop, Harvest, Repair, Attack, and second Build each replaced the pending order with resources still `100000/100000`, 23 units, and zero foundations. | Immediate structured-clone scenario; under 0.5s per in-app Browser call. | Exact in-app Browser result recorded each `ok: true`; pending phase `to-site`, target `null`; retask order matched the command. |
| M01 arrival/cancel | `construction-lifecycle` | Arrival deducted exactly 1200/800 once, created one 120/1200 Hall, and hid the Peasant. Cancel removed it, released the Peasant, and returned 75%, leaving `99700/99800`. | Arrival simulated within the bounded 3000-tick fixture; in-app Browser call under 0.5s. | Unit count `23 -> 24 -> 23`; pending/paid/legacy saves round-tripped; invalid phases and mismatched builders were rejected. |
| Oil-platform source parity | source definitions | Travel remains unpaid; arrival revalidates the complete gate and removes the tanker inside unless the definition explicitly declares `BuilderOutside`. | Focused source/construction guards. | `verify:constructions` and `verify:source-build-action` PASS. |

## Budgets

- Determinism scan: PASS (`verify:runtime-determinism`)
- Average update/render: `0.72ms / 2.89ms` in the foreground in-app Browser session
- Asset gate when applicable: PASS (1182 files)

## Residual risk

- The broad legacy command-card sweep hung after passing M01 on two bounded attempts. The exact-seed `verify:browser-construction-lifecycle` gate exits 0 reliably and passed twice consecutively plus fresh re-review; the unrelated post-M01 harness residual is not a Plan 011 release blocker.

## Review decision

- READY
- Reviewer: `/root/plan011_rereview`
- Reason: All construction lifecycle, oil arrival, deterministic probing, save compatibility/isolation, and exact-seed verifier findings were fixed; final re-review reported no findings.

Review decision: READY
