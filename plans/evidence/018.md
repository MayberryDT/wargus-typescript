# Plan 018 Evidence — Runtime Performance Feedback Loop

## Status

**IN PROGRESS. No matrix row or performance budget verdict is accepted yet.**

Wave 0 is accepted through coordinator closeout `757163f3ea265c1825e66de69799e0d51d41817a`. Plan 018 acceptance is active: the required non-browser entry gates pass, while the hardware-qualified canonical matrix and fixed-tick determinism proof remain pending under the current
[performance contract](../PERFORMANCE-ACCEPTANCE.md). Plans 019–025 remain
dependency-blocked.

## Evidence amendment

On 2026-07-28, the user explicitly authorized replacing Plan 018's obsolete
capture and determinism rules with the canonical
[Halla execution policy](../HALLA-EXECUTION-POLICY.md) and
[performance acceptance contract](../PERFORMANCE-ACCEPTANCE.md). Those shared
contracts now govern browser fallback, qualification, lifecycle, statistics,
matrix validity, determinism, process safety, and durable artifacts.

GBrain page `sessions/2026/07/wargus-plan-018-browser-capture-blocker`
remains the historical blocker authority. This amendment supersedes only its
old duration and wall-clock ending-tick protocol conclusion; its partial-capture,
invalid-artifact, explicit-STOP, and downstream-blocked facts remain true.

### Superseded blocker

The earlier evidence treated three conditions as a combined terminal blocker:

- the in-app Browser did not continuously advance RAF;
- a hard tab-duration cap could not fit the required warmup and heap windows;
  and
- browser repeats ended at different wall-clock-driven world ticks.

That blocker record is retained below as historical diagnostic context, but
its protocol conclusion is superseded:

- headless Chromium is an approved fallback when it satisfies the Halla policy;
- valid captures have no arbitrary tab-duration ceiling;
- the canonical trial uses a 5-second warmup, reset/start at `t0`, checkpoints
  at `t15` and `t30`, and the shared heap formula; and
- deterministic equality is proved in non-browser fixed-tick runs. Browser
  repeats must match initial profile-definition and entity/effect
  fingerprints, not wall-clock ending ticks.

The historical SwiftShader captures remain invalid for frame-budget acceptance
and remain `/tmp`-only diagnostics. This amendment does not relabel them as
durable or accepted evidence.

## Existing implementation record

The Plan 018 implementation is present through `e80215e`:

| Commit | Recorded result |
|---|---|
| `fc41c95` | Bounded runtime metrics, summaries, deterministic profile definitions, and focused verifier. |
| `a105efa` | Scheduler accepted/dropped delta, step, backlog, and timing diagnostics. |
| `7ae81bc` | Smoke-only profile hooks, input/render timing, heap/long-task reporting, and renderer/display-object instrumentation. |
| `3c0eeea` | Corrected tracked display-object churn accounting. |
| `9bcfe2f` | Preserved smoke profile scheduler execution. |
| `bbddb36` | Validated manifest-backed smoke building IDs. |
| `6923c09` | Preserved `command-18` selection and production command seams. |
| `d9e1c63`, `34592b7`, `e80215e` | Historical protocol/evidence records, including the blocker and partial-attempt corrections. |

Renderer instrumentation spans `src/main.ts`, `src/view/renderWorld.ts`,
`src/view/renderHud.ts`, and `src/view/renderOverlays.ts`, backed by
`src/performance/displayObjectPerformance.ts`. The recorded counter scope is
instrumented Pixi scene objects, including registered Wargus bitmap-text
results; texture destruction is excluded.

## Previously recorded green non-browser gates

These are historical results recorded before this amendment, not a substitute
for rerunning the gates at the eventual capture commit:

- `npm run verify:performance-metrics` — passed percentiles, thresholds, ring
  wrap, lifecycle, bounded input pairing, scheduler summaries, deterministic
  profile definitions, manifest-backed building IDs, and display-object churn.
- `npm run verify:playtest-telemetry` — passed distributions, scheduler data,
  strict `smoke=1` gating, long-task filtering, hooks, and scoped scene-object
  registration.
- `npm run verify:simulation-scheduler` — passed accepted/dropped accounting,
  backlog, capture parity, profile lifecycle, command-profile selection,
  production HUD/pointer command seams, and input samples.
- `./node_modules/.bin/tsc --noEmit` — passed.
- `npm run verify:runtime-determinism` — passed under the previous invocation;
  canonical fixed-tick profile proof remains pending.
- `npm run verify:wargus-assets` — passed with 1,182 files.
- `npm run build` — passed.
- `git diff --check` — passed before the previous evidence closeout.

## Historical diagnostic environment

- Host: `halla`.
- Branch: `perf/plan-018-v2`; runtime fixes through `6923c09`, evidence
  corrections through `e80215e`.
- The T3 preview/in-app Browser did not provide continuously advancing RAF.
- The user approved headless Chromium fallback for that attempt.
- Fallback browser: Google Chrome 150 headless on Halla.
- Renderer: ANGLE Vulkan SwiftShader, a software renderer that is invalid for
  hardware frame-budget evidence.
- The Halla user did not have access to render/video devices during that
  attempt.
- An unrelated pre-existing CPU workload remained active and was not touched,
  so the partial samples were also a contended-host observation.
- Vite used port `43218`. The controller tracked and cleaned only its exact
  PIDs. No listener remained on that port.

## Historical attempted protocol

Each attempted row waited for `loadingVisible === false`, then used:

1. 5 seconds of warmup;
2. reset/start at capture offset 0;
3. heap snapshot at +10 seconds;
4. CPU/input snapshot at +15 seconds;
5. stop/final heap snapshot at +20 seconds; and
6. a hard guard that cleaned the tab before the former duration ceiling.

The +10/+20 heap snapshots were never an authorized substitute for the
required heap measurement. They do not satisfy the current `t15`/`t30`
lifecycle or canonical heap formula.

The intended `command-18` attempt required a smoke action plus real
keyboard/mouse input, but the incomplete matrix never reached that row. The
current contract instead requires a disposable synthetic preflight followed
by real-input measured trials at frozen command and sample counts.

## Historical capture attempts

### Cache-prewarmed final attempt

Directory: `/tmp/wargus-perf-018/20260728T054727Z`

- Cache prewarm completed in 3,978 ms.
- `idle-25` run 1 became ready at 4,301 ms and completed at 29,649 ms.
  Artifact:
  `/tmp/wargus-perf-018/20260728T054727Z/idle-25-run1-1280x720.json`.
- `idle-25` run 2 became ready at 4,464 ms but had not completed by
  29,900 ms. It was terminated and cleaned under the historical controller
  guard, so no artifact was produced.

### Earlier corrected attempt

Directory: `/tmp/wargus-perf-018/20260728T054500Z`

- `idle-25` run 1 completed at 28,945 ms. Artifact:
  `/tmp/wargus-perf-018/20260728T054500Z/idle-25-run1-1280x720.json`.
- `idle-25` run 2 completed at 29,560 ms. Artifact:
  `/tmp/wargus-perf-018/20260728T054500Z/idle-25-run2-1280x720.json`.
- The browser checkpoints had different world ticks:

  | Checkpoint | Run 1 tick | Run 2 tick |
  |---|---:|---:|
  | Initial | 12 | 21 |
  | Started | 124 | 141 |
  | +10 seconds | 396 | 413 |
  | +15 seconds | 540 | 549 |
  | Final | 676 | 685 |

  Under the superseded protocol this was called an invalid deterministic
  repeat. Under the canonical contract ending-tick equality is not a browser
  acceptance rule. The trials remain unaccepted for independent reasons:
  software rendering, obsolete lifecycle, too few trials, and non-durable
  artifacts.
- `army-100` completed at 29,638 ms. Artifact:
  `/tmp/wargus-perf-018/20260728T054500Z/army-100-1280x720.json`.
  It remains observational partial evidence only.
- `army-200` then hit an erroneous 4.0-second controller readiness timeout.
- The remaining rows were not captured.

## Historical matrix disposition

No historical row is accepted.

| Profile | Viewport | Available historical evidence | Current disposition |
|---|---:|---|---|
| idle-25 run 1 | 1280×720 | Final-attempt artifact; earlier artifact | NOT ACCEPTED — software renderer, obsolete lifecycle, fewer than three valid trials, `/tmp` only |
| idle-25 run 2 | 1280×720 | No final artifact; earlier artifact | NOT ACCEPTED — software renderer, obsolete lifecycle, fewer than three valid trials, `/tmp` only |
| army-100 | 1280×720 | Earlier artifact only | NOT ACCEPTED — observational partial evidence only |
| army-200 | 1280×720 | None | NOT CAPTURED — historical readiness timeout |
| command-18 | 1280×720 | None | NOT CAPTURED — no disposable preflight or real-input measured evidence |
| combat-100 | 1280×720 | None | NOT CAPTURED |
| command-18 | 1024×768 | None | NOT CAPTURED |

The contract's seven rows include two independent `idle-25` rows at 1280×720;
each row still requires three valid trials. The historical run labels do not
fulfill those rows.

## Pending canonical acceptance

Accepted Wave 0:

- Plan 026 browser execution and resource-monitor foundation is `DONE-VERIFIED`;
- Plan 027 verification-gate repair is `DONE-VERIFIED`; and
- integrated Wave 0 closeout `757163f3ea265c1825e66de69799e0d51d41817a` is the capture-worktree ancestor.

Pending canonical proof and matrix:

The pending canonical matrix has not started; no capture is authorized before the Wave 0 entry gate.

- rerun fixed-tick non-browser determinism twice per deterministic profile and
  record the required exact state/hash equality;
- qualify a hardware renderer, browser, focused/visible document, advancing
  RAF, profile, viewport, and initial fingerprint;
- capture all seven rows with three independent valid trials each;
- use the canonical 5-second warmup and `t0`/`t15`/`t30` lifecycle;
- use disposable synthetic command preflight and the frozen real-input command
  outcome and sample counts;
- calculate the canonical heap formula and per-trial statistics;
- report every row by its worst trial without pooling samples; and
- store raw artifacts, normalized summary, environment/resource records,
  invalid/replacement metadata, controller identity, and verified SHA-256
  checksums under
  `.artifacts/performance/018/<commit>/<UTC-stamp>/`.

There is no accepted row and no accepted pass/fail budget verdict until that
rerun is complete. The eventual normalized evidence must report failures
honestly; Plan 018 may finish with failed baseline budgets, but not with
missing or invalid acceptance evidence.

## Readiness decision

`IN PROGRESS`. The roadmap is approved, Wave 0 is accepted, and the entry gates pass. The fixed-tick determinism proof and complete hardware-qualified three-trial matrix are being produced. Only after both are accepted may `plans/README.md` record `DONE-VERIFIED` and provide the baseline handoff to Plans 019–025.
