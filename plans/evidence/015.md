# Plan 015 Evidence

- Branch: `codex/gameplay-roadmap`
- Implementation commits: `e6be507`, `c43a28c`, `7eb9230`, `66f0ed8`
- Date/time zone: 2026-07-23 (America/Chicago)
- Status: READY — M01/M10, the full Human playable progression, mirrored Orc fixtures, and closeout gates passed
- Source authority: installed Wargus `409a36f7da7b6a162e3f465d98619db9a9d77cbf` and Stratagus `b3b4babe8eb8f908ac9cb25da9c0fd83c67957c5`

## Completed mechanics evidence

| Contract | Result | Evidence |
|---|---|---|
| Fixed-demo roster graph | READY | The allow list adds exactly the six source producers and four Inventor/Alchemist outputs without changing the one-Peasant setup, starting resources, demo units, or upgrade allow list. `verify:fixed-demo-random-ai` passed. |
| Advanced producer cards | READY | A focused live browser fixture exposed the human and Orc advanced pages with a completed Lumber Mill. Inventor/Mage Tower/Church and Alchemist/Temple/Altar were disabled before Castle/Fortress and executable afterward through their real source commands. |
| M10 conversions | READY | Normal Church/Altar research orders converted an existing Knight/Ogre in place, preserving each unit id, and changed direct Paladin/Ogre Mage Barracks training from blocked to executable. |
| M10 direct output completion | READY | Normal production orders completed Paladin, Ogre Mage, Mage, Death Knight, Ballista, Catapult, Flying Machine, Dwarves, Zeppelin, and Goblin Sappers. The fixture verified resource deduction, queue progress, spawn completion, deterministic counts/ids, and recorded supply snapshots without treating queued reservation as correct. |
| M01 replay | READY | An in-app session began with one selected Peasant, no Hall, `10000/5000/5000`, and `1/0` food. At visible 2x the Peasant paid once, entered the real Hall foundation, completed it, emerged, and food became `1/1`. Exact Plan 011 retask/refund evidence remains READY in `plans/evidence/011.md`. |
| Human playable M10 | READY | The resumable F12/Run/Space/F11 chain completed Paladin, Mage, Ballista, Flying Machine, and Dwarves from the original one-Peasant opening. The final tick-41925 slot contains all five outputs, three completed Farms, and empty Barracks/Inventor queues. |

## Budgets

- Determinism scan: PASS (`verify:runtime-determinism`).
- Asset gate: PASS (1,182 files).
- Foreground fixed-demo update/render replay: PASS at `0.72ms / 2.89ms` in M01; the accepted movement replay also remained inside budget at `1.66ms / 8.03ms`.
- Halla headless update: PASS at `2.04ms`. Its Vulkan headless renderer measured `34.93ms` because Chromium remained on a non-foreground headless presentation path; this is recorded as host/tool noise rather than replacing the accepted foreground renderer budget.

Focused M10 browser result:

```text
Advanced tech paths verified (10 trained outputs, 2 in-place conversions).
```

After review correction commit `38dbbc1`, the default train gate also passed
and explicitly executed the advanced fixture before its legacy live-map queue
case:

```text
Advanced tech paths verified (10 trained outputs, 2 in-place conversions, deterministic ids).
Browser train session verified (maps/ladder/Garden of war BNE.pud.smp.gz, unit=unit-peasant, queue=0->1, remaining=null->8.9, tick=350).
```

The corrected fixture records the exact manifest-derived gold/wood/oil delta,
resource stability through completion, queue progress, one spawned id, and one
count delta for every output. A second fresh cloned-world run must reproduce
the same ordered trained-unit ids.

## Resource discipline

The in-app session used one low-priority Vite server and one browser tab. Vite
measured about 0.5% CPU and the game renderer about 10% CPU. Both were closed
immediately after the Hall replay; no project server or browser process
remained.

After the user imposed a strict 30-second live-session ceiling, the execution
gates were amended to use real F11/F12 Save/Load checkpoints between visible
milestones. No live tab, Vite server, headless browser, or verifier process was
left running after that amendment.

Static inspection confirms this is a persistent checkpoint rather than an
in-memory pause: `saveCurrentGame` writes the serialized active world, camera,
control groups, and source viewport state to the active browser storage slot;
`loadCurrentSaveSlot` reconstructs the playable world from that slot. The 2x
setting is preserved through the saved mutable engine setting and restored as
the runtime source-speed multiplier.

The bounded F11/F12 protocol then passed across a full tab and Vite restart:

| Checkpoint | Live tab time | Visible result |
|---|---:|---|
| `checkpoint-save-1` | 6.812s | F11 Save committed slot 1 with one Peasant, no Hall, `10000/5000/5000`, `1/0`, and visible `1.2x`. |
| `checkpoint-load-1` | 4.528s | After stopping and restarting Vite, F12 displayed slot 1 with its saved timestamp; Load restored the same opening state and visible `1.2x`. |
| `checkpoint-opening-2x` | 7.097s | F12 loaded the persisted opening, one visible source-speed step advanced the superseded `1.8x` calibration save to `2x`, and F11 committed the canonical one-Peasant/no-Hall checkpoint. |
| `checkpoint-hall-foundation` | 9.749s | Loaded the canonical `2x` opening, placed the Town Hall through the Peasant's visible basic-build page, observed the paid 10% foundation and `8800/4200/5000`, then saved the in-progress construction. |
| `checkpoint-hall-progress` | 22.612s | Loaded the 10% foundation, clicked visible `Run`, advanced normal `2x` construction to the late-stage Hall frame, confirmed resources remained `8800/4200/5000` and food remained `1/0`, then saved the partial Hall. |
| `checkpoint-hall-complete` | 16.322s | Loaded the late-stage Hall, clicked visible `Run`, observed the Town Hall completion notice and emerged Peasant, confirmed resources remained `8800/4200/5000` and food changed to `1/1`, then saved the completed Hall. |

An earlier multi-call orchestration attempt was discarded after measuring
53.360s, and a 7.593s bundled attempt was discarded because it opened the Save
menu without committing the slot. Neither is acceptance evidence. All accepted
segments closed the tab, stopped Vite, reset the viewport override, and left
ports `5173`, `5211`, and `9235` clear.

Two intermediate source-speed calibration saves (`1.7x` in 9.967s and `1.8x`
in 8.156s) stayed inside the live budget but are superseded by
`checkpoint-opening-2x`; they do not count as gameplay milestones.

F12 Load restores the world with the battle paused. A 20.820s Hall-continuation
attempt was discarded after the visible control remained `Run` and the saved
10% foundation made no progress. All subsequent elapsed-time segments must
click the visible `Run` control after loading; this requirement is now part of
the global execution gate and Plan 015 Task 6.

A later blind Farm segment failed to apply F12 Load and then overwrote slot 1
with the fresh opening. A read-only 3.208s inspection confirmed the current
slot contains only one Peasant, no Hall, `10000/5000/5000`, source speed 30,
and tick 366. The prior Hall foundation/progress/completion rows remain valid
player-visible observations, but their saved states are no longer resumable.
The progression chain must reconstruct the Hall before advancing. Future
segments now assert slot JSON and loaded/target smoke state inside the same
bounded action and close without F11 on any mismatch.

The guard then proved its fail-closed behavior twice without overwriting the
fresh slot. The first reconstruction reached exact `2x` and deducted the
correct Hall resources, then closed before F11 because an under-construction
Hall is hidden from `selectedUnitTypes`. The corrected attempt closed before
mutation because it assumed total `unitCount=18`; the loaded scenario reports
19 total units even though the stable player signature remains one Peasant,
tick 366, and `10000/5000/5000`. Plan 015's two-failure STOP condition now
applies. No further live reconstruction should run until the guard precondition
is reconciled around player-owned slot units/resources/tick rather than a
hard-coded total scenario unit count.

Static source reconciliation identified both bad assumptions. Smoke
`selectedUnitTypes` is only the current selection, smoke `ownedUnitCounts`
filters out every unit with non-null `construction`, and smoke `unitCount`
contains all players and neutral scenario objects. The serialized slot is the
authoritative boundary: it stores every unit (including its player and
construction state), every player's resources, tick, and mutable source speed.
The reviewed guard therefore derives its baseline from the slot, compares the
loaded map-wide count only with that slot's own unit-array length, and proves a
new foundation after F11 from the visibility-player Town Hall record with a
non-null `construction` object. The next live action may use this reconciled
guard; no game or browser process was needed for the review.

The first post-review reconstruction failed closed in 6.608s with a bounded
condition timeout; a 0.513s read-only slot check confirmed the opening remained
unchanged. A separate 2.100s diagnostic then proved F12 itself was correct:
the fresh runtime had 18 units at tick 0, and Load restored the persisted 19
units at tick 366, paused, with source speed 30 and `10000/5000/5000`.

The one allowed corrected reconstruction used HUD-reported control rectangles
instead of fixed speed/Run coordinates and reported its failing stage. It
loaded the exact slot, advanced each source-speed step through 35/40/45/50/55
to 60 (`2x`), clicked Run, and selected the Peasant, then timed out at the
compound basic-build-page assertion in 5.599s. It never placed a Hall and never
opened F11, so the persisted slot remains the fresh opening. This is the second
post-review reconstruction failure; the plan's two-failure STOP condition
applies again. The next turn must statically split that assertion into (1)
command-page transition and (2) actual command-card contents/disabled reason,
then inspect the exact expected Town Hall command id before allowing another
live mutation. Ports `5173`, `5211`, and `9235` were clear afterward.

Static command-path reconciliation confirms the game-side transition is
deterministic. `executeHudCommandForSelection("build-basic-page")` returns
`commandPage: 1`, while `executeSelectionHotkey` maps the source Peasant
`button` value `1` to the same page. The installed manifest's Peasant card has
exactly one level-0 basic-page command (`B`, action `button`, value `1`) and one
level-1 Town Hall command (`H`, action `build`, value `unit-town-hall`, no
allow-rule blocker). Runtime card generation names that build command
`source-build:unit-town-hall`. The failure was therefore an automation
observation seam: it combined page transition and resulting-card availability
after a canvas-coordinate click. The reviewed next action pre-asserts the
visible `B` command, issues its displayed source hotkey, asserts page 1 alone,
then captures and validates the exact `H` command before issuing it. This
reconciliation was static and started no game or browser process.

The reconciled visible-hotkey action then completed the Hall flow and wrote a
valid foundation checkpoint in 6.956s. The post-save guard initially reported
a mismatch because it required the saved tick to equal a target tick observed
while simulation was still running. Inspection of the already captured saved
object (without reopening the game) proved every gameplay field:

| Checkpoint | Tick | Speed | Resources | Player-owned state |
|---|---:|---:|---|---|
| `checkpoint-hall-foundation-guarded` | 502 | source 60 (`2x`) | `8800/4200/5000` | Peasant plus `unit-town-hall-19`, 120 HP, builder inside, 50.133s construction remaining |

The tick mismatch was observational, not save corruption: F11 occurred after
the target observation, so normal running advanced the world before
serialization. This checkpoint is accepted and resumable. The global gate and
Task 6 now require visible Pause before F11 and capture the target tick only
after `paused === true`; future post-save checks can then require exact tick
equality while also reducing live CPU use.

The first continuation from that checkpoint advanced 480 ticks in 11.555s but
was discarded before F11 because the final canvas Pause tap did not toggle the
running state. The accepted foundation slot was therefore not overwritten.
Static input tracing confirms the installed UI documents `Space pauses`, and
`sourceSpeedKeyAction` maps `Space` directly to the same `toggle-pause` state
transition before selection hotkeys run. The next bounded continuation will
use that documented visible hotkey, assert `paused === true`, and only then
open F11; it will not repeat the unreliable canvas pause tap.

The corrected continuation passed in 12.940s. F12 restored the accepted Hall
foundation at tick 502, visible Run advanced exactly 490 ticks at source speed
60 (`2x`), documented `Space` paused the game, and F11 saved the same paused
tick 992. Resources remained `8800/4200/5000`; Town Hall construction advanced
from 50.133s remaining / 120 HP to 33.800s remaining / 404 HP. This is an
accepted resumable in-progress checkpoint, not Hall-completion evidence.

The Hall-completion continuation passed in 21.876s. F12 loaded tick 992 paused,
visible Run advanced normal `2x` construction, and the ready owned-unit count
reported the Hall before documented `Space` paused at tick 2030. F11 saved that
exact tick. The saved `unit-town-hall-19` is complete at 1200/1200 HP with no
construction state; `unit-peasant-16` emerged idle at 30 HP. Resources remained
`8800/4200/5000`, and the visible resource chips reported food `1/1` and one
worker. This is the accepted resumable completed-Hall checkpoint.

The Farm-foundation segment passed in 6.080s. From the completed-Hall save,
the visible Peasant card exposed `B` then source Farm `F`; normal placement
deducted exactly 500 gold / 250 wood. Documented `Space` paused at tick 2245
and F11 saved that exact tick. The accepted checkpoint has
`unit-farm-21` at 40/400 HP with 19.200s construction remaining, resources
`8300/3950/5000`, and visible food still `1/1` until completion.

The Farm-completion continuation passed in 12.407s. F12 restored tick 2245
paused; visible Run completed normal construction, visible food changed from
`1/1` to `1/5`, and documented `Space` paused at tick 2835. F11 saved that
exact tick. `unit-farm-21` is complete at 400/400 HP, and resources remain
`8300/3950/5000`. This is the accepted resumable completed-Farm checkpoint.

A paused four-worker queue setup was discarded after 4.506s before F11. All
four visible `P` inputs reached their expected queue length and 400-gold
deduction, but the final paused-state assertion correctly failed. Static input
tracing shows acknowledged train commands call
`resumeFixedDemoAfterIssuedCommand(true)`, intentionally resuming a loaded
paused fixed demo. The completed-Farm slot was not overwritten. The corrected
setup protocol re-pauses with documented `Space` and verifies paused after
each queued worker before issuing the next input.

The corrected pause-after-each-command setup was also discarded before F11 in
5.197s. All four queue-length, 400-gold, auto-resume, and re-pause checks
passed; the final assertion incorrectly expected visible food `1/5`.
Static source inspection confirms the known Plan 016 seam: `getPlayerSupply`
sums production-queue demand, train admission uses `used + queued`, and the HUD
renders that sum, so the current four-Peasant queue displays `5/5`. Original
Wargus applies demand when each unit is created, and Plan 015 explicitly keeps
this value diagnostic until Plan 016 corrects it. The completed-Farm slot is
still the last accepted save. A future queue setup may assert queue length 4
and resources `6700/3950/5000`, record visible `5/5` as a known temporary
observation, and must not label queued reservation source-faithful.

The reviewed four-worker queue setup passed in 5.895s. F12 loaded the completed
Farm at tick 2835 paused. Each visible Hall `P` deducted exactly 400 gold,
increased the queue by one, auto-resumed as expected, and was immediately
re-paused with documented `Space`. F11 saved tick 2887 with four Peasant orders
(7.267s, 9s, 9s, and 9s remaining), resources `6700/3950/5000`, and current
HUD diagnostic food `5/5`. The queued-food value is recorded only as the known
Plan 016 seam; it is not source-faithful acceptance evidence.

The worker-completion continuation passed in 20.069s. F12 restored the
four-order Hall queue at tick 2887 paused; visible Run completed all four
normal production orders, documented `Space` paused at tick 3935, and F11
saved that exact tick. The Hall queue is empty, resources remain
`6700/3950/5000`, visible food is `5/5`, and the five deterministic worker ids
are `unit-peasant-16`, `unit-peasant-23`, `unit-peasant-24`,
`unit-peasant-25`, and `unit-peasant-26`.

The second-Farm foundation segment passed in 4.684s. From the five-worker save,
visible Peasant `B` then source Farm `F` placed `unit-farm-27`; normal arrival
deducted exactly 500 gold / 250 wood. Documented `Space` paused at tick 4063
and F11 saved that exact tick with the new Farm at 40/400 HP and 19.867s
construction remaining. Resources are `6200/3700/5000`, and food correctly
remains `5/5` until completion.

The second-Farm completion passed in 13.250s. F12 restored tick 4063 paused;
visible Run completed normal construction, supply changed from `5/5` to
`5/9`, documented `Space` paused at tick 4665, and F11 saved that exact tick.
Both Farms are complete at 400/400 HP, and resources remain
`6200/3700/5000`.

The Barracks-foundation segment passed in 5.278s. The visible Peasant basic
page exposed source Barracks `B`; normal placement deducted exactly 700 gold /
450 wood. Documented `Space` paused at tick 4839 and F11 saved that exact tick
with `unit-human-barracks-28` at 80/800 HP and 39.400s construction remaining.
Resources are `5500/3250/5000`, and supply remains `5/9`.

The Barracks-completion continuation passed in 22.567s. F12 restored tick 4839
paused; visible Run completed normal construction, documented `Space` paused
at tick 6032, and F11 saved that exact tick. `unit-human-barracks-28` is
complete at 800/800 HP, resources remain `5500/3250/5000`, and supply remains
`5/9`. Selecting the Hall after completion exposed enabled source command
`K`, `source-upgrade:unit-keep`, with the installed cost/status
`Time 200, Gold 2000, Wood 1000, Oil 200`, proving the real Barracks dependency
unlocked Keep without a bypass.

The Keep-upgrade start passed in 3.289s. F12 loaded tick 6032 paused, selecting
the Hall exposed the verified source `K` command, and issuing it deducted
exactly 2000 gold / 1000 wood / 200 oil. The acknowledged order auto-resumed;
documented `Space` re-paused at tick 6040, and F11 saved that exact tick with
one `unit-keep` transformation order at 39.733s / 40s remaining. Resources are
`3500/2250/4800`, and supply remains `5/9`.

The Keep-completion continuation passed in 22.972s. F12 restored tick 6040
paused; visible Run completed the normal transformation, documented `Space`
paused at tick 7237, and F11 saved that exact tick. The original Hall id
`unit-town-hall-19` is now `unit-keep` at 1400/1400 HP with an empty queue.
Resources remain `3500/2250/4800`, and supply remains `5/9`.

The first Lumber Mill foundation attempt was discarded in 4.393s before
resource deduction or F11. The installed Peasant basic-page `L` command was
enabled and entered build placement, but every candidate derived from the
first Peasant's opening-era screen offsets was rejected after the base had
grown to a Keep, Barracks, two Farms, and five workers. The accepted Keep slot
remains current. The next turn must inspect the loaded worker/building screen
points, choose an idle Peasant with the greatest live clearance, and generate
bounded placement rings from the current base rather than reusing opening
offsets.

A 2.326s read-only layout capture then mapped the current base and preserved
the save unchanged: the playable map pane ends above the lower HUD at roughly
`y=484`, while several rejected opening-era candidates had landed on those HUD
panels. The grown base occupies screen `x=286..694`, `y=203..448`; all five
idle workers were identified by id and screen/world point.

The layout-aware Lumber Mill retry passed in 4.533s. It selected idle
`unit-peasant-25`, entered visible Peasant `B` then source Mill `L`, and used
the verified upper clearing at screen `(450,90)` / world `(1424,3696)`.
Normal arrival deducted exactly 600 gold / 450 wood. Documented `Space` paused
at tick 7373 and F11 saved that exact tick with
`unit-elven-lumber-mill-29` at 60/600 HP and 29.567s construction remaining.
Resources are `2900/1800/4800`, and supply remains `5/9`.

The Lumber Mill completion passed in 17.302s. F12 restored tick 7373 paused;
visible Run completed normal construction, documented `Space` paused at tick
8266, and F11 saved that exact tick. `unit-elven-lumber-mill-29` is complete
at 600/600 HP. Resources remain `2900/1800/4800`, and supply remains `5/9`.

The first Blacksmith foundation attempt was discarded in 6.343s before
resource deduction or F11. The visible Peasant `B` then source Blacksmith `S`
command was enabled and entered placement, but the candidate set was rejected
after the newly completed Mill occupied the upper clearing. The accepted
completed-Mill slot remains current. Per the grown-base placement gate, the
next continuation must first capture the updated worker/building layout and
derive a new free placement region; it must not retry the rejected grid.

A 1.634s read-only post-Mill capture showed the completed Mill at screen
`(422,40)` and its released builder `unit-peasant-25` at world `(1424,3632)`,
just north of the current camera. Static input tracing confirmed the visible
minimap centers the camera on a clicked tile.

The north-clearing Blacksmith retry passed in 4.869s. A visible minimap pan to
tile `(44,112)` exposed `unit-peasant-25`; visible `B` then source Blacksmith
`S` placed `unit-human-blacksmith-30` at screen `(440,294.4)` / world
`(1296,3536)`. Normal arrival deducted exactly 800 gold / 450 wood / 100 oil.
Documented `Space` paused at tick 8395 and F11 saved that exact tick with the
Blacksmith at 77/775 HP and 39.733s construction remaining. Resources are
`2100/1350/4700`, and supply remains `5/9`.

The Blacksmith completion passed in 23.230s. F12 restored tick 8395 paused;
visible Run completed normal construction, documented `Space` paused at tick
9602, and F11 saved that exact tick. `unit-human-blacksmith-30` is complete at
775/775 HP. Resources remain `2100/1350/4700`, and supply remains `5/9`.

A 1.643s read-only post-Blacksmith capture found released
`unit-peasant-25` at world `(1296,3472)`, immediately north of the completed
Blacksmith and outside the default camera.

The Stables-foundation segment passed in 4.369s. A visible minimap pan to tile
`(40,108)` exposed that worker; visible advanced page `V` then source Stables
`A` placed `unit-stables-31` at screen `(640,160)` / world `(1296,3344)`.
Normal arrival deducted exactly 1000 gold / 300 wood. Documented `Space`
paused at tick 9705 and F11 saved that exact tick with the Stables at 50/500 HP
and 29.267s construction remaining. Resources are `1100/1050/4700`, and
supply remains `5/9`.

The Stables completion passed in 17.836s. F12 restored tick 9705 paused;
visible Run completed normal construction, documented `Space` paused at tick
10590, and F11 saved that exact tick. `unit-stables-31` is complete at 500/500
HP. Resources remain `1100/1050/4700`, and supply remains `5/9`. Lumber Mill,
Blacksmith, and Stables now satisfy the installed Castle dependency graph; the
remaining blocker is the honest resource shortfall.

The Stables completion passed in 17.836s. F12 restored tick 9705 paused;
visible Run completed normal construction, documented `Space` paused at tick
10590, and F11 saved that exact tick. `unit-stables-31` is complete at 500/500
HP. Resources remain `1100/1050/4700`, and supply remains `5/9`. All three
installed Castle prerequisites are now complete; progression must gather the
remaining 1400 gold and 150 wood normally before the Castle command can run.

A 1.704s read-only economy capture mapped the real nearby resources without
issuing orders or changing the save. The visible nearest source is
`unit-gold-mine-15` at world `(1712,3888)` with 60000 gold. Verified Forest
tiles include `(41,116)` / world `(1328,3728)` beside the southern workers and
`(42,101)` / world `(1360,3248)` beside northern `unit-peasant-25`. The planned
normal setup assigns Peasants 16/24/26 to that mine and 23/25 to those Forest
tiles through visible minimap pans, selections, and right-click orders.

Two subsequent assignment attempts never reached the app: fresh in-app tabs
timed out during localhost navigation after 10.021s and 10.020s respectively,
before F12, gameplay input, or F11. The server was stopped after each, the
Stables-complete slot remains unchanged, and the browser troubleshooting guide
requires keeping the existing browser binding and trying a fresh tab later
rather than switching control mechanisms. The two-failure STOP condition
applies for this turn; these are browser-navigation failures, not economy or
gameplay failures.

The next continuation's fresh tab again timed out during localhost navigation
after 10.026s, before F12, gameplay input, or F11. The server was stopped and
the Stables-complete slot remains unchanged. This is the same external browser
navigation condition across two consecutive goal turns; it has not yet met the
three-turn blocked threshold.

The third consecutive goal-turn audit repeated the same failure: a fresh tab
in the required existing in-app Browser binding timed out navigating to
localhost after 10.012s, before F12, gameplay input, or F11. Vite was confirmed
ready first and stopped immediately afterward; ports were clear and the
accepted Stables-complete slot remained unchanged. Repository policy requires
the in-app Browser and the Browser recovery guide forbids switching mechanisms
for this condition. The three-turn blocked threshold is therefore satisfied:
Plan 015 and its dependent roadmap plans are BLOCKED until the in-app Browser
can navigate to the local app again.

Tyler explicitly resumed the blocked work and requested another bounded retry.
The first audit of that resumed run used one fresh in-app Browser tab and one
low-priority Vite server. Navigation again timed out after 10 seconds, before
F12, gameplay input, or F11. The server was stopped immediately, all project
ports were clear, and the accepted Stables-complete slot remains unchanged.
This is failure 1 of the fresh resumed-blocked audit; no alternate browser
mechanism was used.

The next one-tab retry restored the required in-app Browser path. One
low-priority Vite server loaded the Stables-complete checkpoint through F12 at
tick `10590`, paused, 2x, with `1100/1050/4700` and the expected Keep,
Barracks, two Farms, Elven Lumber Mill, Human Blacksmith, Stables, and five
Peasants. Three Peasants received normal gold-mine harvest orders and two
received normal Forest harvest orders. A delayed pause after the fourth order
allowed normal gathering to advance resources to `1430/1050/4700`; the fifth
order was then issued and the visible Pause control stopped the game. F11
saved exact paused tick `11046`. Slot inspection confirmed all five saved
`harvest` orders (three gold, two wood), the new resources, and a changed
`savedAt`. The only tab was closed, the server stopped, and all project
ports were clear. Plan 015 is unblocked and normal gathering can continue from
this checkpoint toward `11600/4100`.

A second one-tab gathering segment restored tick `11046`, clicked the visible
Run control, and advanced normal 2x simulation for 12 seconds. The visible
Pause control stopped the game at tick `11781`; F11 then saved that exact tick
with `2640/1050/4700`. Slot inspection again confirmed the same five normal
`harvest` orders (three gold, two wood). The tab and low-priority server
were closed immediately and all project ports were clear. Gold progressed by
`1210`; wood had not yet completed a return cycle during this segment and
remained diagnostic for the next bounded continuation.

The next bounded one-tab segment resolved that diagnostic without a code
change. F12 restored tick `11781`; the two lumber Peasants were both actively
gathering and already carried `86` and `60` wood. After 12 seconds of visible
2x Run, their normal return cycles raised wood from `1050` to `1300` while the
three miners raised gold from `2640` to `3740`. The visible Pause control
stopped the game and F11 saved exact tick `12532` with `3740/1300/4700` and
both wood workers still on normal `harvest` orders. This proves the earlier
flat wood total was only an incomplete return cycle, not a harvesting failure.
The only tab and low-priority server were closed and all project ports cleared.

The following economy continuation restored tick `12532` and retained all
five harvest orders. A missed first Pause click under rendering load extended
the intended 12-second run to about 18 seconds, still below the hard 30-second
session ceiling. The second visible Pause click stopped exact tick `13635`;
F11 saved `5390/1425/4700` with the same three gold and two wood orders. The
single tab and low-priority server were then closed and ports cleared.

A guarded 12-second continuation restored tick `13635`; its pause action was
checked and retried only if needed before F11. The first Pause click succeeded,
and F11 saved exact tick `14370` with `6380/1550/4700`. The one tab and
low-priority server were closed and all project ports cleared.

The next attempted continuation loaded that checkpoint, but the sole in-app
Browser tab disappeared during the bounded Run action before Pause or F11
could be confirmed. The low-priority server was stopped immediately and the
unsaved segment was discarded under the Task 6 checkpoint rule. Tick `14370`
with `6380/1550/4700` remains the last accepted state; no alternate browser or
parallel process was started.

The recovery retry shortened live simulation to six seconds so total tab life
would remain comfortably bounded. F12 restored the untouched tick `14370`
checkpoint, visible Run/Pause completed normally, and F11 saved exact tick
`14744` with `7040/1675/4700`. Total live tab time was about nine seconds. The
tab and low-priority server were closed and all project ports cleared. Six
seconds is now the default economy-segment duration.

The next six-second segment restored tick `14744` and saved exact paused tick
`15119` with `7590/1675/4700`. No lumber return completed within this short
window; all five existing harvest orders remained the accepted continuation.
The one tab and low-priority server were closed and ports cleared.

The following six-second segment restored tick `15119` and saved exact paused
tick `15495` with `8030/1675/4700`. The sole tab and low-priority server were
closed immediately and all project ports cleared.

The next six-second segment restored tick `15495` and saved exact paused tick
`15868` with `8690/1800/4700`. A Browser telemetry-network warning did not
affect the local game, guarded save, or cleanup. The single tab and low-priority
server were closed and all project ports cleared.

The third six-second segment in this continuation restored tick `15868` and
saved exact paused tick `16238` with `9240/1925/4700`. The single tab and
low-priority server were closed and all project ports cleared.

The next six-second segment restored tick `16238` and saved exact paused tick
`16616` with `9790/1925/4700`. The single tab and low-priority server were
closed and all project ports cleared.

The following six-second segment restored tick `16616` and saved exact paused
tick `16991` with `10340/1925/4700`. The single tab and low-priority server
were closed and all project ports cleared.

The third segment restored tick `16991`; two early Pause clicks were missed
under rendering load, and the guarded third click stopped exact tick `17769`,
still well below the 30-second live-session ceiling. F11 saved
`11550/2175/4700`, leaving gold only `50` below the full front-loaded target.
The single tab and low-priority server were closed and all project ports
cleared.

The first miner-retask attempt crossed the live gold target, but clicking
Peasant 16 at its diagnostic screen point did not select it. A later browser
control timeout ended the unsaved segment, so the server and stale tab were
closed and tick `17769` remained authoritative. Code inspection explains the
interaction: `findSelectableUnitAt` and rectangle selection deliberately
exclude `isUnitInsideResourceSource(unit)`, matching workers being hidden and
unselectable inside a mine, while `browserSmokeOwnedUnitScreenPoints` includes
all owned live units and therefore is not a selectable-point contract. The
next retry must retask Peasant 26 while it is visibly outside the mine, then
cross the remaining gold target and select Peasants 24/16 only after their
return phases make them visible. No gameplay fix is required for this source-
faithful mine behavior.

The next retry split the retask into a single-worker checkpoint. F12 restored
tick `17769`; Peasant 26 was still in its saved `to-resource` phase outside
the mine at `(1544.8,3888)`, and a visible map click selected it successfully.
A visible right-click on Peasant 23's active southern Forest target issued a
normal wood `harvest` order. The acknowledged command advanced only to tick
`17818` before the visible Pause control stopped it. F11 saved exact tick
`17818` with `11550/2175/4700`: Peasants 16/24 remained on gold, while
23/25/26 were on wood. The sole tab and low-priority server were closed and
all project ports cleared.

The immediate Peasant 24 follow-up restored tick `17818` and visibly located
the worker in its saved `to-dropoff` phase, but combining selection, minimap
pan, target order, and pause in one browser-control action exceeded that
action's timeout before F11. The low-priority server and stale tab were closed,
and the unsaved attempt was discarded. Tick `17818` remains authoritative.
The next retry will split selection, target pan, right-click verification,
pause, and save into separate short browser actions, matching the successful
Peasant 26 sequence.

The split retry exposed a real port fidelity bug instead of an interaction
mistake. At tick `18265`, Peasant 24 reached the northern forest but became
idle still carrying `100 gold`. Installed Stratagus resolves the edge
unambiguously: `src/ui/mouse.cpp` accepts the different-resource order, and
`src/action/action_resource.cpp` calls `DropResource(unit)` when gathering the
new resource starts, then switches `CurrentResource`. The previous cargo is
discarded rather than delivered. The TypeScript order entered gathering with
zero remaining capacity, gathered nothing, and cleared itself.

A focused resource-action verifier was first tightened to require that source
contract and failed on the two missing port fragments. The minimal runtime
correction now drops mismatched carried cargo at the transition from
`to-resource` to `gathering`. The same focused gate then passed, followed by
`tsc --noEmit`.

One short visible replay loaded the saved economy, selected Peasant 24 with
`gold 100`, and issued a normal Lumber/Mine command onto the northern forest.
The worker visibly changed to `wood 70` while remaining in `Chop Wood ...
gathering`; the old gold was not credited. F11 saved exact tick `19559` with
`12430 gold / 2549 wood / 4700 oil`. Direct read-only slot inspection confirmed
all five assignments: Peasant 16 gathering gold, and Peasants 23/24/25/26
gathering wood. Peasant 24's saved order targets forest tile `(44, 103)` with
`70 wood` carried. The sole tab and low-priority server were closed after the
checkpoint.

A follow-up tried to catch Peasant 16 during its brief visible mine-exit phase.
Read-only polling detected the exit, but rendering lag delayed the visible
Pause action until the worker had already re-entered the mine. Rather than
repeat the timing-sensitive interaction, the accepted progress was paused and
saved at exact tick `22282` with `13750/3173/4700`. Slot inspection confirmed
Peasant 16 still gathering gold and Peasants 23/24/25/26 still assigned to
wood. Only `927 wood` remains to the front-loaded `4100` target, so retasking
the miner is unnecessary. The tab's wall-clock lifetime exceeded the intended
30-second cap because several Pause clicks were delayed; future gameplay tabs
use a hard 20-second wall-clock cutoff and no exit-window polling. The sole tab
and low-priority server were then closed.

A continuation audit reconciled Tasks 1–5 against the live implementation and
this packet, marked their plan checkboxes complete, and reran
`verify:fixed-demo-random-ai` successfully. The current slot guard also exposed
that tick `22282` carries source speed `55` (`1.833x`) from the abandoned miner
timing attempt. It remains the authoritative state, but the next accepted
segment must visibly restore `2x` before F11.

Three resource-bounded attempts from that slot were discarded without F11.
The first reached the remaining wood target live, but coordinate-level Pause
input lagged past the hard cutoff. A focused canvas-key diagnostic confirmed
the in-app browser cannot deliver locator-scoped key presses to the Pixi
canvas. A final low-level keyboard attempt was rejected as unsupported by the
in-app browser before it could pause. Each tab and low-priority server was
closed immediately; slot 1 remains exact tick `22282` at
`13750/3173/4700`. No more live retry was made in this continuation.

The next continuation isolated the browser-input ambiguity with one four-second
fresh-game diagnostic. Exact `tab.cua.keypress({ keys: ["Space"] })` changed
the live state from running to paused after a 500ms observation window. The
earlier uppercase spelling and immediate reads were the automation error; the
game's documented Pause handler was not defective.

That proven input path then advanced the accepted economy through four guarded
segments. Every segment loaded the prior exact slot with F12, restored or
confirmed visible `2x`, used visible Run, paused with exact `Space`, required
`paused === true`, saved with F11, and verified the slot tick and source speed
60 before closing the sole tab and low-priority server:

| Accepted checkpoint | Exact tick | Resources |
|---|---:|---:|
| `checkpoint-economy-23498` | 23498 | `14410/3421/4700` |
| `checkpoint-economy-24750` | 24750 | `14960/3669/4700` |
| `checkpoint-economy-25865` | 25865 | `15510/3793/4700` |
| `checkpoint-economy-27014` | 27014 | `16060/4043/4700` |

The saved speed drift is corrected, the full gold budget is exceeded, and only
`57 wood` remains before the front-loaded `4100` target. No tab lived longer
than 20 seconds, and ports `5173`, `5211`, and `9235` were clear after every
segment.

Static preparation for the next milestone confirmed the installed Castle
contract: the Keep exposes source key `C`, action `upgrade-to`, value
`unit-castle`; the Castle costs `2500 gold / 1200 wood / 500 oil` and 200
source time. No dependency bypass or placement step is involved.

Two final-wood attempts were discarded without F11. The first F12 interaction
failed closed at fresh tick `0`. The second loaded tick `27014`, but its Run
click was delayed under rendering load: after the nominal 13-second wait the
world had advanced only 17 ticks, and the following Space observation still
reported running. The tab closed before the 20-second cap, leaving slot 1
unchanged. This exposed a protocol violation rather than a game-state defect:
Task 6 already requires confirming Run changed away from its paused state
before any elapsed-time wait. The next segment must poll only that
acknowledgement for at most two seconds and close if it is absent; its runtime
budget begins only after `paused === false` is observed.

The corrected Run-acknowledgement protocol completed the resource budget.
F12 loaded tick `27014`; the visible Run control acknowledged at tick `27018`
inside the two-second guard. After normal `2x` gathering, exact Space paused at
tick `28414` with `16830/4167/4700`, exceeding both front-loaded targets. F11
saved the same tick and source speed 60. The sole tab lived under 20 seconds
and the low-priority server was closed.

The next independent milestone started the real Castle transformation. Static
manifest inspection established Keep source key `C`, action `upgrade-to`,
value `unit-castle`, and cost `2500 gold / 1200 wood / 500 oil`. F12 restored
tick `28414`; selecting the visible Keep exposed exactly that enabled command.
Visible `C` deducted the exact cost, auto-resumed, and exact Space re-paused at
tick `28435`. F11 saved the original `unit-town-hall-19` as a Keep with one
39.3-second transformation order and resources `14330/2967/4200`.

One guarded completion segment loaded tick `28435`, required visible Run to
acknowledge at tick `28441`, and advanced normal `2x` simulation. Exact Space
paused at tick `29953`; the owned counts contained one Castle and no Keep. F11
saved that exact tick with the original id `unit-town-hall-19` transformed to
`unit-castle`, 1600/1600 HP, an empty queue, and resources
`15120/3217/4200`. The Castle milestone is complete. Each tab remained below
20 seconds and all project ports were cleared afterward.

Church preparation confirmed a `3x3`, 700-HP source building costing
`900 gold / 500 wood`, exposed from the Peasant advanced page by source key
`C`. A read-only Castle-era layout capture recorded the current owned centers:
Castle `(1440,3904)`, Farms `(1344,3904)` and `(1536,3936)`, Barracks
`(1584,3792)`, Mill `(1424,3696)`, Blacksmith `(1296,3536)`, and Stables
`(1296,3344)`. Peasants 24 and 25 were visible north of the base.

The first Church placement attempt loaded tick `29953`, selected visible
Peasant 24, opened advanced page `V`, and verified the enabled
`source-build:unit-church` command (`C`, `build`, `unit-church`). Two visible
placement previews remained red: the first intersected fog/trees and the
Castle edge; the second intersected a tree and the Blacksmith. Neither click
cleared the pending build command, created a unit, advanced the tick, or
deducted resources. Per the grown-base placement gate, the tab closed without
F11 and the exact tick-29953 Castle save remains authoritative. Those candidate
coordinates must not be reused; the next layout capture should inspect a fresh
east or south clearing and move the pointer away from the map edge immediately
after previewing to prevent edge-scroll drift.

A fresh southern capture exposed a large explored clearing west of the Castle.
The accepted Church retry loaded tick `29953`, selected visible Peasant 24,
opened advanced page `V`, and used the visible Church button after verifying
its enabled source contract. Moving the pending preview to screen `(150,300)`
showed a fully green `3x3` footprint. The visible click created
`unit-church-32` at world `(1296,3696)` and initially deducted the exact
`900/500` cost at tick `30028`; the pointer immediately returned to map center
to stop edge-scroll drift.

Exact Space paused and F11 saved tick `30838`. Normal gathering during the
acknowledged construction raised resources to `14700/2904/4200`; the saved
Church was 538/700 HP with 8.067 seconds remaining, and Peasant 24 was inside
as its builder. A short completion segment loaded that exact state, required
visible Run acknowledgement, completed the Church at normal `2x`, and paused
at tick `31135`. F11 saved `unit-church-32` complete at 700/700 HP with no
construction state, Peasant 24 present again, and resources
`14820/2904/4200`. The Church milestone is complete. Each tab stayed below 20
seconds and all project ports were cleared afterward.

Mage Tower preparation confirmed a source `3x3`, 500-HP building costing
`1000 gold / 200 wood`, available on advanced page `V`. A Church-era southern
capture centered the explored field so the existing Church sat east of a new
open candidate. Peasant 24 opened the real advanced page; the enabled visible
Mage Tower button armed placement, and the preview at screen `(250,250)` was
fully green.

The visible click created `unit-mage-tower-33` at world `(1136,3664)` and
initially deducted the exact cost at tick `31177`, leaving
`13820/2704/4200`. The pointer returned to map center immediately. Exact Space
then paused after normal construction had already completed; F11 saved exact
tick `32581` at source speed 60 with the Tower 500/500 HP, no construction
state, Peasant 24 present, and resources `14540/2829/4200` after concurrent
normal gathering. The single under-20-second segment therefore satisfied both
foundation and completion for its one Mage Tower milestone. All project ports
were cleared afterward.

Static budgeting then expanded the economy target from the Castle minimum to
the actual remaining Human showcase. Installed manifest costs for Castle,
Church, Mage Tower, Inventor, Knight, Paladin research, Mage, Ballista, Flying
Machine, and Dwarves total `10500 gold / 3050 wood / 500 oil`. Starting from
the saved `1100/1050/4700`, front-loaded normal gathering should reach at least
`11600 gold / 4100 wood`. The live saved world disproved the plan's predicted
10-food cap: after Mage, Ballista, and Flying Machine the Dwarves command was
disabled at the actual 9-food cap. A third Farm was therefore built through
the normal Peasant command card, exactly as the plan permits when live source
supply differs; Dwarves became executable afterward without a dependency or
supply bypass.

One earlier continuous 2x session reached a visible, source-command-built Town
Hall, two Farms, four additional Peasants, Barracks, Elven Lumber Mill, and
Blacksmith. Normal gathering raised resources while those buildings completed,
and the Hall's Keep command was visible when the session was stopped. Because
the run was interrupted before a save checkpoint, this is observational
evidence only and does not satisfy Task 6 or provide resumable state.

## Final Human progression

- Ballista queued through the visible Barracks `B` command (`Time 250, Gold
  900, Wood 300`) and completed as `unit-ballista-37` at tick 39070.
- Flying Machine queued through the visible Inventor `F` command (`Time 65,
  Gold 500, Wood 100`) and completed as `unit-balloon-38` at tick 39690.
- The live 9-food cap disabled Dwarves, so Peasant `unit-peasant-23` opened the
  basic build page, placed a third Farm through the visible Farm command, and
  completed `unit-farm-39` at tick 40346.
- Dwarves then queued through the enabled Inventor `D` command (`Time 200,
  Gold 700, Wood 250`) and completed as `unit-dwarves-40` at tick 41925.
- Final slot resources are `13100 gold / 2866 wood / 4200 oil`; all five Human
  showcase outputs exist and the Barracks and Inventor queues are empty.

Accepted artifact hashes:

- Ballista: `21e4f5b878345ae30aa1523c8c2064f607bfeb6cc223bee16febe9e1913bc3d2`
- Flying Machine: `90798e2e8bf8a999c738f1829b43d95fce9dba01e46c3c6f63d8e6619d276dcd`
- Supply Farm: `fe98539aa0e7dfb6a83203ca21c3cc4d802aaec8490a1420cbc42fff19a0dd45`
- Dwarves/final: `f8347cc55060a10a3399a25ca0270f98e8a477bd940a1db22de9f7f613b7467e`

The final one-tab Halla browser fixture again completed all ten Human/Orc
advanced outputs, both conversion paths, deterministic spawned ids, and all
six producer cards before/after top-tier prerequisites in 10.0 seconds.

## Review decision

- Implementation review: READY after `38dbbc1`
- Reviewer: `/root/plan015_serial_review`
- Findings corrected: default advanced-gate coverage, per-output exact resource
  evidence, deterministic trained ids, and duplicated lifecycle setup
- Overall Plan 015 acceptance: READY. The earlier independent implementation
  review remains valid, and the missing player-visible progression has now
  completed with guarded persistent checkpoints.

## Default command-card gate status

The advanced-only producer-card gate passes in about five seconds. The default
command-card gate exposed shared-live-world contamination: repair failed late
but passed in isolation, and after one reset the same pattern moved to the
spell group. Commit `9398eaa` gives every stateful fixture group a fresh fixed
demo. A bounded default run then passed transport/tanker, training, patrol,
all pending actions, eight harvest-rally producers, 48 research commands, 50
spell commands, and 79-type/407-command parity. It reached the unchanged source
execution matrix before the external five-minute cap expired without a test
failure. The cap killed the parent before normal cleanup; the exact dedicated
Chrome/Vite groups were identified and terminated immediately.

The committed advanced-only gate remains green after the isolation change.
The architecture failure is resolved. A resource-bounded focused execution
run then executed 342 commands and classified all 65 disabled commands. It
identified two newly exposed, expected prerequisite cases: Inventor Flying
Machine requires Elven Lumber Mill and Alchemist Zeppelin requires Troll
Lumber Mill in the source dependency data. The classification now records
those alongside the existing targeted prerequisite fixtures.

The subsequent focused hotkey run passed with 44 unique hotkeys executed, 65
disabled cases classified, 7 ambiguous source-key collisions skipped, and 291
duplicate coverage cases deduplicated. It also passed the six advanced-producer
checks. The verifier now treats an enabled command sharing a key with a
disabled, higher-precedence source command as ambiguous, matching runtime
dispatch (notably Alchemist Zeppelin/Stop on `Z`). Both focused runs used one
low-priority browser and cleaned up ports `5211` and `9235` normally.

The exact default `npm run verify:browser-command-card-session` command then
passed in one continuous low-priority run. It verified 79 types / 407 expected
commands, 342 click executions / 65 classified disabled cases, 44 unique
hotkey executions / 65 disabled / 7 ambiguous / 291 duplicate cases, and all
six advanced producers. The process exited zero and cleaned up ports `5211`
and `9235` normally. The full progression session remains required before the
plan can be marked DONE.

Review decision: PENDING

## Inventor playable checkpoint

- Continued the authoritative one-Peasant playable save from tick 32581 at
  visible 2x speed using one short-lived in-app Browser tab.
- Selected Peasant 24 through the real command card, opened the advanced build
  page, and placed the Inventor with a fully green 3x3 preview at world
  coordinates `(1168, 3760)`.
- Paused and saved at tick 34473 with resources `14620 gold / 2741 wood / 4200
  oil`.
- The saved Inventor is `unit-inventor-34`, complete at `500/500` HP with no
  construction state; Peasant 24 is present after completing the building.
- The source command cost remains `1000 gold / 400 wood`. The exact live
  placement delta was not used as cost evidence because workers continued
  gathering during the short placement observation.
- The tab was closed immediately after verification, the low-priority Vite
  process was stopped, and ports `5173`, `5211`, and `9235` were clear.

The next playable checkpoint is advanced-unit production: Knight followed by
Paladin progression, Mage, Ballista, Flying Machine, and Dwarves.

## Knight playable checkpoint

- Loaded the authoritative tick-34473 Inventor save through F12 and required
  exact agreement on map, tick, source speed 60 (visible 2x), resources, and
  the save-derived total unit count before issuing input.
- Selected `unit-human-barracks-28` through the visible map, then required the
  enabled source train command `source-train:unit-knight`: displayed key `K`,
  action `train-unit`, value `unit-knight`, status
  `TRAIN KNIGHT (Time 90, Gold 800, Wood 100)`.
- The acknowledged order resumed play, queued one Knight with 17.9667 seconds
  remaining, and deducted the exact cost: resources changed from
  `14620/2741/4200` to `13820/2641/4200` before further simulation.
- Normal production completed and spawned `unit-knight-35` at world
  `(1520, 3792)` with `90/90` HP and no active order. The Barracks queue was
  empty.
- Paused with `Space` and saved through F11 at exact tick 35120, source speed
  60, resources `14180 gold / 2641 wood / 4200 oil`; the saved slot tick matched
  the paused runtime exactly.
- The accepted tab was open for about 16 seconds. It closed before the server
  was stopped, and ports `5173`, `5211`, and `9235` were clear afterward.

Three preceding attempts were discarded without overwriting the tick-34473
slot: two guards rejected incorrect/too-early load observations, and one timing
probe showed that 10 wall seconds advanced only 15.9 of the required 18
simulation seconds on this machine. One earlier automation-timeout recovery
kept that discarded tab open for about 35 seconds; it is not acceptance
evidence. The accepted flow now uses one cleanup-protected action, a 25-second
automation ceiling, and a 12.5-second Knight production window.

The next playable milestone is Church Paladin research converting
`unit-knight-35` in place, followed by verification that direct Barracks
Paladin training becomes enabled.

## Paladin research in-progress checkpoint

- Loaded the authoritative tick-35120 Knight save through F12 and required
  exact agreement on map, tick, source speed 60 (visible 2x), resources,
  player-owned Knight count, and the save-derived total unit count.
- The save also contained an unrelated player-1 Light Axes research entry.
  Player-0 guards intentionally ignored it without mutating it; it completed
  normally while this segment ran.
- Selected `unit-church-32` through the visible map, then required the enabled
  source research command `source-research:upgrade-paladin`: displayed key
  `P`, action `research`, value `upgrade-paladin`, status
  `UPGRADES KNIGHTS TO PALADINS (Gold 1000)`.
- The acknowledged order resumed play, created one player-0 Paladin research
  entry, and deducted the exact cost: resources changed from
  `14180/2641/4200` to `13180/2641/4200` before further simulation.
- After the bounded progression window, `unit-knight-35` remained a Knight and
  the Paladin research remained active, so no conversion was claimed.
- Paused with `Space` and saved through F11 at exact tick 35846, source speed
  60, resources `13540 gold / 2766 wood / 4200 oil`. The saved player-0 entry
  is `upgrade-paladin` at `unit-church-32`, with `25.8/50` seconds remaining.
- The accepted tab was open for about 23 seconds. It closed before the
  low-priority server was stopped, and ports `5173`, `5211`, and `9235` were
  clear afterward.

The next segment must resume this exact active research rather than issue the
command again. Completion must preserve id `unit-knight-35` while changing its
type to `unit-paladin`, then expose enabled direct Paladin training at the
Barracks.

## Paladin conversion complete checkpoint

- Loaded the authoritative tick-35846 active-research save through F12 and
  required exact agreement on map, tick, source speed 60 (visible 2x),
  resources, total unit count, `unit-knight-35`, and the player-0 Paladin
  research entry at `25.8/50` seconds remaining.
- Used the visible `Run` control after F12 and asserted the runtime changed from
  paused to running. No second research command was issued.
- Normal simulation completed the research. The ready-owned counts changed
  from one Knight and zero Paladins to zero Knights and one Paladin.
- Paused with `Space`, then selected `unit-human-barracks-28` through the
  visible map. Its command card exposed enabled direct training
  `source-train:unit-paladin`: displayed key `P`, action `train-unit`, value
  `unit-paladin`, status `TRAIN PALADIN (Time 90, Gold 800, Wood 100)`.
- Saved through F11 at exact tick 36829, source speed 60, resources
  `14140 gold / 2891 wood / 4200 oil`. The player-0 research list is empty and
  the saved unit retains id `unit-knight-35` while its type is now
  `unit-paladin` with `90/90` HP.
- The accepted tab was open for about 24 seconds. It closed before the
  low-priority server was stopped, and ports `5173`, `5211`, and `9235` were
  clear afterward.

This proves both halves of the playable source mechanic: conversion preserves
the existing unit id, and completion unlocks separate direct Paladin training.
The next advanced-output milestone is one Mage from `unit-mage-tower-33`.

## Mage production in-progress checkpoint

- Loaded the authoritative tick-36829 Paladin save through F12 and required
  exact agreement on map, tick, source speed 60 (visible 2x), resources, total
  unit count, one Paladin, and zero Mages.
- Selected `unit-mage-tower-33` through the visible map, then required the
  enabled source train command `source-train:unit-mage`: displayed key `T`,
  action `train-unit`, value `unit-mage`, status
  `TRAIN MAGE (Time 120, Gold 1200)`.
- The acknowledged order resumed play, created one 24-second Mage queue entry,
  and deducted the exact cost: resources changed from `14140/2891/4200` to
  `12940/2891/4200` before further simulation.
- Paused with `Space` and saved through F11 at exact tick 37387, source speed
  60, resources `13180 gold / 2891 wood / 4200 oil`. No Mage was claimed yet;
  the saved Mage Tower queue contains one `unit-mage` entry with exactly
  `5.4/24` seconds remaining.
- The accepted tab was open for about 14 seconds. The long action returned
  after saving while paused, then a separate close call shut the tab before
  the low-priority server was stopped. Ports `5173`, `5211`, and `9235` were
  clear afterward.

A preceding full-completion attempt hit its automation ceiling during cleanup.
The server was stopped, the stale tab was closed, and a separate read-only slot
check proved it had not overwritten tick 36829. That discarded recovery ran
past the intended browser ceiling and is not acceptance evidence. The accepted
flow now saves before returning and performs tab shutdown in a separate short
call, preventing cleanup latency from consuming the gameplay budget.

The next segment must load tick 37387 and resume this exact Mage queue without
issuing another train command. Completion must spawn one normal Mage and leave
the Mage Tower queue empty.

## Mage production complete checkpoint (Halla)

- Transferred only the accepted Wargus save-slot JSON to the isolated Halla
  workspace, then loaded tick 37387 through the real F12 Load Game menu and its
  visible Load button. The loaded runtime matched the slot exactly: paused,
  source speed 60 (visible 2x), 36 units, one Paladin, zero Mages, and resources
  `13180 gold / 2891 wood / 4200 oil`.
- Clicked the visible `Run` control and allowed normal simulation to finish the
  existing Mage Tower queue without issuing a second train command. Paused with
  `Space`, selected visible `unit-mage-tower-33` at screen point `640,360`, and
  observed one Mage with an empty production queue before saving.
- Saved through the real F11 Save Game menu and its visible Save button at exact
  tick 37566, source speed 60, resources `13300 gold / 3016 wood / 4200 oil`,
  and 37 total units. The committed slot contains `unit-mage-36` and no Mage
  Tower queue entry.
- The accepted artifact is
  `/home/halla/workspaces/Wargus-TypeScript-artifacts/save-slot-mage-complete.json`
  with SHA-256
  `9c9556e393c8549307c4bc1808f0eba766d96c9919aa1d85deaf999160bcf992`.
  The one-tab browser used Halla preview port 54251 at low priority. The save
  committed before the outer SSH action reached its cleanup ceiling; the exact
  recorded Playwright/Chromium/Vite process tree was then cleared, and port
  54251 was confirmed free.

The next playable milestone is one Ballista from `unit-human-barracks-28`.
