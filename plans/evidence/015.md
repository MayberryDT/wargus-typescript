# Plan 015 Evidence

- Branch: `codex/gameplay-roadmap`
- Implementation commits: `e6be507`, `c43a28c`, `7eb9230`
- Date/time zone: 2026-07-10 (America/Denver)
- Status: IN PROGRESS
- Source authority: installed Wargus `409a36f7da7b6a162e3f465d98619db9a9d77cbf` and Stratagus `b3b4babe8eb8f908ac9cb25da9c0fd83c67957c5`

## Completed mechanics evidence

| Contract | Result | Evidence |
|---|---|---|
| Fixed-demo roster graph | READY | The allow list adds exactly the six source producers and four Inventor/Alchemist outputs without changing the one-Peasant setup, starting resources, demo units, or upgrade allow list. `verify:fixed-demo-random-ai` passed. |
| Advanced producer cards | READY | A focused live browser fixture exposed the human and Orc advanced pages with a completed Lumber Mill. Inventor/Mage Tower/Church and Alchemist/Temple/Altar were disabled before Castle/Fortress and executable afterward through their real source commands. |
| M10 conversions | READY | Normal Church/Altar research orders converted an existing Knight/Ogre in place, preserving each unit id, and changed direct Paladin/Ogre Mage Barracks training from blocked to executable. |
| M10 direct output completion | READY | Normal production orders completed Paladin, Ogre Mage, Mage, Death Knight, Ballista, Catapult, Flying Machine, Dwarves, Zeppelin, and Goblin Sappers. The fixture verified resource deduction, queue progress, spawn completion, deterministic counts/ids, and recorded supply snapshots without treating queued reservation as correct. |
| M01 replay | READY | An in-app session began with one selected Peasant, no Hall, `10000/5000/5000`, and `1/0` food. At visible 2x the Peasant paid once, entered the real Hall foundation, completed it, emerged, and food became `1/1`. Exact Plan 011 retask/refund evidence remains READY in `plans/evidence/011.md`. |

Focused M10 browser result:

```text
Advanced tech paths verified (10 trained outputs, 2 in-place conversions).
```

After review correction commit `38dbbc1`, the default train gate also passed
and explicitly executed the advanced fixture before its legacy live-map queue
case:

```text
Advanced tech paths verified (10 trained outputs, 2 in-place conversions, deterministic ids).
Browser train session verified (maps/ladder/Garden of war BNE.pud.smp.gz, unit=unit-peasant, queue=0->1, remaining=null->8.9, tick=350).
```

The corrected fixture records the exact manifest-derived gold/wood/oil delta,
resource stability through completion, queue progress, one spawned id, and one
count delta for every output. A second fresh cloned-world run must reproduce
the same ordered trained-unit ids.

## Resource discipline

The in-app session used one low-priority Vite server and one browser tab. Vite
measured about 0.5% CPU and the game renderer about 10% CPU. Both were closed
immediately after the Hall replay; no project server or browser process
remained.

After the user imposed a strict 30-second live-session ceiling, the execution
gates were amended to use real F11/F12 Save/Load checkpoints between visible
milestones. No live tab, Vite server, headless browser, or verifier process was
left running after that amendment.

Static inspection confirms this is a persistent checkpoint rather than an
in-memory pause: `saveCurrentGame` writes the serialized active world, camera,
control groups, and source viewport state to the active browser storage slot;
`loadCurrentSaveSlot` reconstructs the playable world from that slot. The 2x
setting is preserved through the saved mutable engine setting and restored as
the runtime source-speed multiplier.

The bounded F11/F12 protocol then passed across a full tab and Vite restart:

| Checkpoint | Live tab time | Visible result |
|---|---:|---|
| `checkpoint-save-1` | 6.812s | F11 Save committed slot 1 with one Peasant, no Hall, `10000/5000/5000`, `1/0`, and visible `1.2x`. |
| `checkpoint-load-1` | 4.528s | After stopping and restarting Vite, F12 displayed slot 1 with its saved timestamp; Load restored the same opening state and visible `1.2x`. |
| `checkpoint-opening-2x` | 7.097s | F12 loaded the persisted opening, one visible source-speed step advanced the superseded `1.8x` calibration save to `2x`, and F11 committed the canonical one-Peasant/no-Hall checkpoint. |
| `checkpoint-hall-foundation` | 9.749s | Loaded the canonical `2x` opening, placed the Town Hall through the Peasant's visible basic-build page, observed the paid 10% foundation and `8800/4200/5000`, then saved the in-progress construction. |
| `checkpoint-hall-progress` | 22.612s | Loaded the 10% foundation, clicked visible `Run`, advanced normal `2x` construction to the late-stage Hall frame, confirmed resources remained `8800/4200/5000` and food remained `1/0`, then saved the partial Hall. |
| `checkpoint-hall-complete` | 16.322s | Loaded the late-stage Hall, clicked visible `Run`, observed the Town Hall completion notice and emerged Peasant, confirmed resources remained `8800/4200/5000` and food changed to `1/1`, then saved the completed Hall. |

An earlier multi-call orchestration attempt was discarded after measuring
53.360s, and a 7.593s bundled attempt was discarded because it opened the Save
menu without committing the slot. Neither is acceptance evidence. All accepted
segments closed the tab, stopped Vite, reset the viewport override, and left
ports `5173`, `5211`, and `9235` clear.

Two intermediate source-speed calibration saves (`1.7x` in 9.967s and `1.8x`
in 8.156s) stayed inside the live budget but are superseded by
`checkpoint-opening-2x`; they do not count as gameplay milestones.

F12 Load restores the world with the battle paused. A 20.820s Hall-continuation
attempt was discarded after the visible control remained `Run` and the saved
10% foundation made no progress. All subsequent elapsed-time segments must
click the visible `Run` control after loading; this requirement is now part of
the global execution gate and Plan 015 Task 6.

A later blind Farm segment failed to apply F12 Load and then overwrote slot 1
with the fresh opening. A read-only 3.208s inspection confirmed the current
slot contains only one Peasant, no Hall, `10000/5000/5000`, source speed 30,
and tick 366. The prior Hall foundation/progress/completion rows remain valid
player-visible observations, but their saved states are no longer resumable.
The progression chain must reconstruct the Hall before advancing. Future
segments now assert slot JSON and loaded/target smoke state inside the same
bounded action and close without F11 on any mismatch.

The guard then proved its fail-closed behavior twice without overwriting the
fresh slot. The first reconstruction reached exact `2x` and deducted the
correct Hall resources, then closed before F11 because an under-construction
Hall is hidden from `selectedUnitTypes`. The corrected attempt closed before
mutation because it assumed total `unitCount=18`; the loaded scenario reports
19 total units even though the stable player signature remains one Peasant,
tick 366, and `10000/5000/5000`. Plan 015's two-failure STOP condition now
applies. No further live reconstruction should run until the guard precondition
is reconciled around player-owned slot units/resources/tick rather than a
hard-coded total scenario unit count.

Static source reconciliation identified both bad assumptions. Smoke
`selectedUnitTypes` is only the current selection, smoke `ownedUnitCounts`
filters out every unit with non-null `construction`, and smoke `unitCount`
contains all players and neutral scenario objects. The serialized slot is the
authoritative boundary: it stores every unit (including its player and
construction state), every player's resources, tick, and mutable source speed.
The reviewed guard therefore derives its baseline from the slot, compares the
loaded map-wide count only with that slot's own unit-array length, and proves a
new foundation after F11 from the visibility-player Town Hall record with a
non-null `construction` object. The next live action may use this reconciled
guard; no game or browser process was needed for the review.

The first post-review reconstruction failed closed in 6.608s with a bounded
condition timeout; a 0.513s read-only slot check confirmed the opening remained
unchanged. A separate 2.100s diagnostic then proved F12 itself was correct:
the fresh runtime had 18 units at tick 0, and Load restored the persisted 19
units at tick 366, paused, with source speed 30 and `10000/5000/5000`.

The one allowed corrected reconstruction used HUD-reported control rectangles
instead of fixed speed/Run coordinates and reported its failing stage. It
loaded the exact slot, advanced each source-speed step through 35/40/45/50/55
to 60 (`2x`), clicked Run, and selected the Peasant, then timed out at the
compound basic-build-page assertion in 5.599s. It never placed a Hall and never
opened F11, so the persisted slot remains the fresh opening. This is the second
post-review reconstruction failure; the plan's two-failure STOP condition
applies again. The next turn must statically split that assertion into (1)
command-page transition and (2) actual command-card contents/disabled reason,
then inspect the exact expected Town Hall command id before allowing another
live mutation. Ports `5173`, `5211`, and `9235` were clear afterward.

Static command-path reconciliation confirms the game-side transition is
deterministic. `executeHudCommandForSelection("build-basic-page")` returns
`commandPage: 1`, while `executeSelectionHotkey` maps the source Peasant
`button` value `1` to the same page. The installed manifest's Peasant card has
exactly one level-0 basic-page command (`B`, action `button`, value `1`) and one
level-1 Town Hall command (`H`, action `build`, value `unit-town-hall`, no
allow-rule blocker). Runtime card generation names that build command
`source-build:unit-town-hall`. The failure was therefore an automation
observation seam: it combined page transition and resulting-card availability
after a canvas-coordinate click. The reviewed next action pre-asserts the
visible `B` command, issues its displayed source hotkey, asserts page 1 alone,
then captures and validates the exact `H` command before issuing it. This
reconciliation was static and started no game or browser process.

The reconciled visible-hotkey action then completed the Hall flow and wrote a
valid foundation checkpoint in 6.956s. The post-save guard initially reported
a mismatch because it required the saved tick to equal a target tick observed
while simulation was still running. Inspection of the already captured saved
object (without reopening the game) proved every gameplay field:

| Checkpoint | Tick | Speed | Resources | Player-owned state |
|---|---:|---:|---|---|
| `checkpoint-hall-foundation-guarded` | 502 | source 60 (`2x`) | `8800/4200/5000` | Peasant plus `unit-town-hall-19`, 120 HP, builder inside, 50.133s construction remaining |

The tick mismatch was observational, not save corruption: F11 occurred after
the target observation, so normal running advanced the world before
serialization. This checkpoint is accepted and resumable. The global gate and
Task 6 now require visible Pause before F11 and capture the target tick only
after `paused === true`; future post-save checks can then require exact tick
equality while also reducing live CPU use.

The first continuation from that checkpoint advanced 480 ticks in 11.555s but
was discarded before F11 because the final canvas Pause tap did not toggle the
running state. The accepted foundation slot was therefore not overwritten.
Static input tracing confirms the installed UI documents `Space pauses`, and
`sourceSpeedKeyAction` maps `Space` directly to the same `toggle-pause` state
transition before selection hotkeys run. The next bounded continuation will
use that documented visible hotkey, assert `paused === true`, and only then
open F11; it will not repeat the unreliable canvas pause tap.

The corrected continuation passed in 12.940s. F12 restored the accepted Hall
foundation at tick 502, visible Run advanced exactly 490 ticks at source speed
60 (`2x`), documented `Space` paused the game, and F11 saved the same paused
tick 992. Resources remained `8800/4200/5000`; Town Hall construction advanced
from 50.133s remaining / 120 HP to 33.800s remaining / 404 HP. This is an
accepted resumable in-progress checkpoint, not Hall-completion evidence.

The Hall-completion continuation passed in 21.876s. F12 loaded tick 992 paused,
visible Run advanced normal `2x` construction, and the ready owned-unit count
reported the Hall before documented `Space` paused at tick 2030. F11 saved that
exact tick. The saved `unit-town-hall-19` is complete at 1200/1200 HP with no
construction state; `unit-peasant-16` emerged idle at 30 HP. Resources remained
`8800/4200/5000`, and the visible resource chips reported food `1/1` and one
worker. This is the accepted resumable completed-Hall checkpoint.

The Farm-foundation segment passed in 6.080s. From the completed-Hall save,
the visible Peasant card exposed `B` then source Farm `F`; normal placement
deducted exactly 500 gold / 250 wood. Documented `Space` paused at tick 2245
and F11 saved that exact tick. The accepted checkpoint has
`unit-farm-21` at 40/400 HP with 19.200s construction remaining, resources
`8300/3950/5000`, and visible food still `1/1` until completion.

The Farm-completion continuation passed in 12.407s. F12 restored tick 2245
paused; visible Run completed normal construction, visible food changed from
`1/1` to `1/5`, and documented `Space` paused at tick 2835. F11 saved that
exact tick. `unit-farm-21` is complete at 400/400 HP, and resources remain
`8300/3950/5000`. This is the accepted resumable completed-Farm checkpoint.

A paused four-worker queue setup was discarded after 4.506s before F11. All
four visible `P` inputs reached their expected queue length and 400-gold
deduction, but the final paused-state assertion correctly failed. Static input
tracing shows acknowledged train commands call
`resumeFixedDemoAfterIssuedCommand(true)`, intentionally resuming a loaded
paused fixed demo. The completed-Farm slot was not overwritten. The corrected
setup protocol re-pauses with documented `Space` and verifies paused after
each queued worker before issuing the next input.

The corrected pause-after-each-command setup was also discarded before F11 in
5.197s. All four queue-length, 400-gold, auto-resume, and re-pause checks
passed; the final assertion incorrectly expected visible food `1/5`.
Static source inspection confirms the known Plan 016 seam: `getPlayerSupply`
sums production-queue demand, train admission uses `used + queued`, and the HUD
renders that sum, so the current four-Peasant queue displays `5/5`. Original
Wargus applies demand when each unit is created, and Plan 015 explicitly keeps
this value diagnostic until Plan 016 corrects it. The completed-Farm slot is
still the last accepted save. A future queue setup may assert queue length 4
and resources `6700/3950/5000`, record visible `5/5` as a known temporary
observation, and must not label queued reservation source-faithful.

The reviewed four-worker queue setup passed in 5.895s. F12 loaded the completed
Farm at tick 2835 paused. Each visible Hall `P` deducted exactly 400 gold,
increased the queue by one, auto-resumed as expected, and was immediately
re-paused with documented `Space`. F11 saved tick 2887 with four Peasant orders
(7.267s, 9s, 9s, and 9s remaining), resources `6700/3950/5000`, and current
HUD diagnostic food `5/5`. The queued-food value is recorded only as the known
Plan 016 seam; it is not source-faithful acceptance evidence.

The worker-completion continuation passed in 20.069s. F12 restored the
four-order Hall queue at tick 2887 paused; visible Run completed all four
normal production orders, documented `Space` paused at tick 3935, and F11
saved that exact tick. The Hall queue is empty, resources remain
`6700/3950/5000`, visible food is `5/5`, and the five deterministic worker ids
are `unit-peasant-16`, `unit-peasant-23`, `unit-peasant-24`,
`unit-peasant-25`, and `unit-peasant-26`.

The second-Farm foundation segment passed in 4.684s. From the five-worker save,
visible Peasant `B` then source Farm `F` placed `unit-farm-27`; normal arrival
deducted exactly 500 gold / 250 wood. Documented `Space` paused at tick 4063
and F11 saved that exact tick with the new Farm at 40/400 HP and 19.867s
construction remaining. Resources are `6200/3700/5000`, and food correctly
remains `5/5` until completion.

The second-Farm completion passed in 13.250s. F12 restored tick 4063 paused;
visible Run completed normal construction, supply changed from `5/5` to
`5/9`, documented `Space` paused at tick 4665, and F11 saved that exact tick.
Both Farms are complete at 400/400 HP, and resources remain
`6200/3700/5000`.

The Barracks-foundation segment passed in 5.278s. The visible Peasant basic
page exposed source Barracks `B`; normal placement deducted exactly 700 gold /
450 wood. Documented `Space` paused at tick 4839 and F11 saved that exact tick
with `unit-human-barracks-28` at 80/800 HP and 39.400s construction remaining.
Resources are `5500/3250/5000`, and supply remains `5/9`.

The Barracks-completion continuation passed in 22.567s. F12 restored tick 4839
paused; visible Run completed normal construction, documented `Space` paused
at tick 6032, and F11 saved that exact tick. `unit-human-barracks-28` is
complete at 800/800 HP, resources remain `5500/3250/5000`, and supply remains
`5/9`. Selecting the Hall after completion exposed enabled source command
`K`, `source-upgrade:unit-keep`, with the installed cost/status
`Time 200, Gold 2000, Wood 1000, Oil 200`, proving the real Barracks dependency
unlocked Keep without a bypass.

The Keep-upgrade start passed in 3.289s. F12 loaded tick 6032 paused, selecting
the Hall exposed the verified source `K` command, and issuing it deducted
exactly 2000 gold / 1000 wood / 200 oil. The acknowledged order auto-resumed;
documented `Space` re-paused at tick 6040, and F11 saved that exact tick with
one `unit-keep` transformation order at 39.733s / 40s remaining. Resources are
`3500/2250/4800`, and supply remains `5/9`.

The Keep-completion continuation passed in 22.972s. F12 restored tick 6040
paused; visible Run completed the normal transformation, documented `Space`
paused at tick 7237, and F11 saved that exact tick. The original Hall id
`unit-town-hall-19` is now `unit-keep` at 1400/1400 HP with an empty queue.
Resources remain `3500/2250/4800`, and supply remains `5/9`.

The first Lumber Mill foundation attempt was discarded in 4.393s before
resource deduction or F11. The installed Peasant basic-page `L` command was
enabled and entered build placement, but every candidate derived from the
first Peasant's opening-era screen offsets was rejected after the base had
grown to a Keep, Barracks, two Farms, and five workers. The accepted Keep slot
remains current. The next turn must inspect the loaded worker/building screen
points, choose an idle Peasant with the greatest live clearance, and generate
bounded placement rings from the current base rather than reusing opening
offsets.

A 2.326s read-only layout capture then mapped the current base and preserved
the save unchanged: the playable map pane ends above the lower HUD at roughly
`y=484`, while several rejected opening-era candidates had landed on those HUD
panels. The grown base occupies screen `x=286..694`, `y=203..448`; all five
idle workers were identified by id and screen/world point.

The layout-aware Lumber Mill retry passed in 4.533s. It selected idle
`unit-peasant-25`, entered visible Peasant `B` then source Mill `L`, and used
the verified upper clearing at screen `(450,90)` / world `(1424,3696)`.
Normal arrival deducted exactly 600 gold / 450 wood. Documented `Space` paused
at tick 7373 and F11 saved that exact tick with
`unit-elven-lumber-mill-29` at 60/600 HP and 29.567s construction remaining.
Resources are `2900/1800/4800`, and supply remains `5/9`.

The Lumber Mill completion passed in 17.302s. F12 restored tick 7373 paused;
visible Run completed normal construction, documented `Space` paused at tick
8266, and F11 saved that exact tick. `unit-elven-lumber-mill-29` is complete
at 600/600 HP. Resources remain `2900/1800/4800`, and supply remains `5/9`.

The first Blacksmith foundation attempt was discarded in 6.343s before
resource deduction or F11. The visible Peasant `B` then source Blacksmith `S`
command was enabled and entered placement, but the candidate set was rejected
after the newly completed Mill occupied the upper clearing. The accepted
completed-Mill slot remains current. Per the grown-base placement gate, the
next continuation must first capture the updated worker/building layout and
derive a new free placement region; it must not retry the rejected grid.

A 1.634s read-only post-Mill capture showed the completed Mill at screen
`(422,40)` and its released builder `unit-peasant-25` at world `(1424,3632)`,
just north of the current camera. Static input tracing confirmed the visible
minimap centers the camera on a clicked tile.

The north-clearing Blacksmith retry passed in 4.869s. A visible minimap pan to
tile `(44,112)` exposed `unit-peasant-25`; visible `B` then source Blacksmith
`S` placed `unit-human-blacksmith-30` at screen `(440,294.4)` / world
`(1296,3536)`. Normal arrival deducted exactly 800 gold / 450 wood / 100 oil.
Documented `Space` paused at tick 8395 and F11 saved that exact tick with the
Blacksmith at 77/775 HP and 39.733s construction remaining. Resources are
`2100/1350/4700`, and supply remains `5/9`.

The Blacksmith completion passed in 23.230s. F12 restored tick 8395 paused;
visible Run completed normal construction, documented `Space` paused at tick
9602, and F11 saved that exact tick. `unit-human-blacksmith-30` is complete at
775/775 HP. Resources remain `2100/1350/4700`, and supply remains `5/9`.

A 1.643s read-only post-Blacksmith capture found released
`unit-peasant-25` at world `(1296,3472)`, immediately north of the completed
Blacksmith and outside the default camera.

The Stables-foundation segment passed in 4.369s. A visible minimap pan to tile
`(40,108)` exposed that worker; visible advanced page `V` then source Stables
`A` placed `unit-stables-31` at screen `(640,160)` / world `(1296,3344)`.
Normal arrival deducted exactly 1000 gold / 300 wood. Documented `Space`
paused at tick 9705 and F11 saved that exact tick with the Stables at 50/500 HP
and 29.267s construction remaining. Resources are `1100/1050/4700`, and
supply remains `5/9`.

The Stables completion passed in 17.836s. F12 restored tick 9705 paused;
visible Run completed normal construction, documented `Space` paused at tick
10590, and F11 saved that exact tick. `unit-stables-31` is complete at 500/500
HP. Resources remain `1100/1050/4700`, and supply remains `5/9`. Lumber Mill,
Blacksmith, and Stables now satisfy the installed Castle dependency graph; the
remaining blocker is the honest resource shortfall.

The Stables completion passed in 17.836s. F12 restored tick 9705 paused;
visible Run completed normal construction, documented `Space` paused at tick
10590, and F11 saved that exact tick. `unit-stables-31` is complete at 500/500
HP. Resources remain `1100/1050/4700`, and supply remains `5/9`. All three
installed Castle prerequisites are now complete; progression must gather the
remaining 1400 gold and 150 wood normally before the Castle command can run.

A 1.704s read-only economy capture mapped the real nearby resources without
issuing orders or changing the save. The visible nearest source is
`unit-gold-mine-15` at world `(1712,3888)` with 60000 gold. Verified Forest
tiles include `(41,116)` / world `(1328,3728)` beside the southern workers and
`(42,101)` / world `(1360,3248)` beside northern `unit-peasant-25`. The planned
normal setup assigns Peasants 16/24/26 to that mine and 23/25 to those Forest
tiles through visible minimap pans, selections, and right-click orders.

Two subsequent assignment attempts never reached the app: fresh in-app tabs
timed out during localhost navigation after 10.021s and 10.020s respectively,
before F12, gameplay input, or F11. The server was stopped after each, the
Stables-complete slot remains unchanged, and the browser troubleshooting guide
requires keeping the existing browser binding and trying a fresh tab later
rather than switching control mechanisms. The two-failure STOP condition
applies for this turn; these are browser-navigation failures, not economy or
gameplay failures.

One earlier continuous 2x session reached a visible, source-command-built Town
Hall, two Farms, four additional Peasants, Barracks, Elven Lumber Mill, and
Blacksmith. Normal gathering raised resources while those buildings completed,
and the Hall's Keep command was visible when the session was stopped. Because
the run was interrupted before a save checkpoint, this is observational
evidence only and does not satisfy Task 6 or provide resumable state.

## Pending acceptance

Plan 015 is not DONE and this packet is not yet READY because the full playable
progression session remains outstanding:

- continue from one Peasant through Keep/Castle at visible 2x;
- produce Paladin, Mage, Ballista, Flying Machine, and Dwarves through the real UI;
- observe the mirrored five Orc paths;
- run the remaining final closeout gates and obtain an overall READY decision.

The long session was deliberately deferred after the Hall replay because it
requires several minutes of gathering while the user's Codex desktop process
was already CPU-saturated. Focused fixture evidence does not substitute for
that remaining player-visible session.

## Review decision

- Implementation review: READY after `38dbbc1`
- Reviewer: `/root/plan015_serial_review`
- Findings corrected: default advanced-gate coverage, per-output exact resource
  evidence, deterministic trained ids, and duplicated lifecycle setup
- Overall Plan 015 acceptance: PENDING because the full playable progression
  remains outstanding

## Default command-card gate status

The advanced-only producer-card gate passes in about five seconds. The default
command-card gate exposed shared-live-world contamination: repair failed late
but passed in isolation, and after one reset the same pattern moved to the
spell group. Commit `9398eaa` gives every stateful fixture group a fresh fixed
demo. A bounded default run then passed transport/tanker, training, patrol,
all pending actions, eight harvest-rally producers, 48 research commands, 50
spell commands, and 79-type/407-command parity. It reached the unchanged source
execution matrix before the external five-minute cap expired without a test
failure. The cap killed the parent before normal cleanup; the exact dedicated
Chrome/Vite groups were identified and terminated immediately.

The committed advanced-only gate remains green after the isolation change.
The architecture failure is resolved. A resource-bounded focused execution
run then executed 342 commands and classified all 65 disabled commands. It
identified two newly exposed, expected prerequisite cases: Inventor Flying
Machine requires Elven Lumber Mill and Alchemist Zeppelin requires Troll
Lumber Mill in the source dependency data. The classification now records
those alongside the existing targeted prerequisite fixtures.

The subsequent focused hotkey run passed with 44 unique hotkeys executed, 65
disabled cases classified, 7 ambiguous source-key collisions skipped, and 291
duplicate coverage cases deduplicated. It also passed the six advanced-producer
checks. The verifier now treats an enabled command sharing a key with a
disabled, higher-precedence source command as ambiguous, matching runtime
dispatch (notably Alchemist Zeppelin/Stop on `Z`). Both focused runs used one
low-priority browser and cleaned up ports `5211` and `9235` normally.

The exact default `npm run verify:browser-command-card-session` command then
passed in one continuous low-priority run. It verified 79 types / 407 expected
commands, 342 click executions / 65 classified disabled cases, 44 unique
hotkey executions / 65 disabled / 7 ambiguous / 291 duplicate cases, and all
six advanced producers. The process exited zero and cleaned up ports `5211`
and `9235` normally. The full progression session remains required before the
plan can be marked DONE.

Review decision: PENDING
