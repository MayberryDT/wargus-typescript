# Plan 017 Evidence

- Branch: `codex/gameplay-roadmap`
- Implementation baseline: `65bfd1ae035e2e2511e3d10c7b53838f5c742a0b`
- Integrated acceptance baseline: `a17bfa7380cbbb256c6f951b86952726f3111259`
- Date/time zone: 2026-07-24, America/Chicago
- Execution host: `halla`
- Decision: **DONE — READY under the final ordinary-play user override**

## Implementation

The fixed demo now selects deterministic ordered start pairs whose enemy source
slot uses `wc2-land-attack`, filters them to candidate B's 70–110-tile band,
and falls back to the pair closest to 90 tiles with player-id tie-breaks. New
fixed-demo worlds use 45 source ticks/sec and display the resulting 1.5x global
pace. The former hidden 1.3x per-unit movement mutation, frame-loop reapplication,
and private unit fields are removed; the smoke compatibility field reports 1.
Loaded saves continue to derive the visible speed from their persisted world and
do not receive the fresh-world presentation default.

The one-Peasant/one-Peon/no-Hall opening, `10,000 gold / 5,000 wood / 5,000 oil`,
normal speed controls, source costs/times/unit stats, deterministic `demoSeed`
and `smoke` seeds, and non-demo air-AI map play remain unchanged.

## Candidate projection

The live generated setup at
`public/wargus/maps/setups/191-Garden_of_war_BNE.pud.sms.json` reproduced the
plan's frozen projection exactly: 42 eligible ordered land-AI pairs; source
enemy slots 1 and 6 excluded as `wc2-air-attack`; band counts A=11, B=13, C=17;
and all nine documented seed/pair mappings unchanged.

Candidate B is the browser-free implementation checkpoint:

| Seed | Human -> enemy | Enemy AI | Distance |
|---|---:|---|---:|
| `plan017-b-min-10` | 6 -> 5 | `wc2-land-attack` | 70.000 |
| `plan017-b-target-21` | 0 -> 7 | `wc2-land-attack` | 93.134 |
| `plan017-b-max-0` | 5 -> 7 | `wc2-land-attack` | 109.490 |

This selection uses the plan's existing champion because the user explicitly
deferred browser execution. It is not presented as an 18-run bakeoff score.

## Browser-free gate results

- Focused land-AI/start-band/global-pace fixture: PASS, including all three representative seeds, deterministic repeats, closest-target fallback, no-land-AI diagnostic, opening premise, and source speed.
- Playtest telemetry static contract: PASS.
- Runtime determinism: PASS; no random or wall-clock APIs under `src/**/*.ts`.
- Save schema/backward compatibility: PASS; 50 persisted `WorldState` fields.
- Browser-script syntax (no browser launched): PASS for both changed scripts.
- Standalone TypeScript: PASS.
- Explicit Wargus asset gate: PASS; 1,182 files checked in the only explicit invocation.
- Production build: PASS; 765 modules transformed. The build lifecycle's mandatory prebuild asset hook repeated the 1,182-file check automatically. It ran on the initial compile-stopped attempt and again on the corrected successful build; neither was a second explicit asset command.
- No hidden movement pace symbols under `src`: PASS.
- `git diff --check`: PASS.

The first consolidated attempt caught a removed `enemySource` lookup that the
existing `startView` fallback still needs. TypeScript and the build stopped on
that missing name; the lookup was restored, then the full corrected browser-free
pass completed with exit 0. Both attempts and every exact root/child PID remain
in `gates.log` rather than being hidden.

Exact commands, PIDs, timestamps, and output belong to the single package at
`/home/halla/workspaces/Wargus-TypeScript-artifacts/plan017-evidence/`.

## Original-source authority

The installed-source rulings in `plans/ORIGINAL-WARGUS-SOURCE.md` remain the
authority: one-Peasant/high-resource setup values are source options, Garden of
War source slots 1 and 6 use air AI, and the distance band/global pace are an
explicit TypeScript-demo tuning choice. Halla does not contain the document's
hardcoded `/home/tyler/...` Stratagus source root; source-comparison scripts
such as `verify-source-speed-keys.mjs` are therefore unavailable here and were
not replaced or weakened.

## Final integrated ordinary play

The accepted session is
`/home/halla/workspaces/Wargus-TypeScript-artifacts/final-integrated-play/attempt-7-ordinary-integrated-2026-07-25T00-02-54Z/`.
It used one fresh 1280×720 page on port `56281`, seed
`plan017-b-target-21`, and commit `a17bfa7`; page wall time was `143,759 ms`.

- The opening remained one selected Peasant, no Hall, and
  `10,000 gold / 5,000 wood / 5,000 oil`. The HUD showed the real default
  `45` source ticks/sec / `1.5x` pace, and the compatibility movement multiplier
  remained `1`.
- The precomputed matchup was human `(116.5,116.5)` to enemy
  `(121.5,23.5)`, distance `93.134` tiles, inside candidate B's 70–110 band.
  The live opponent reported `strategy: land` and source id
  `wc2-land-attack` throughout.
- The Town Hall order accepted at page `9,421 ms` and completed at
  `46,848 ms`, a `37.427 s` ordinary placement-to-completion interval inside
  the 40-second Hall ceiling. The run then completed Farm, Barracks, two extra
  worker queue entries, and a Footman while also exercising harvesting and
  explicit dropoff.
- This economy/legibility-oriented sequence completed its Footman at page
  `117,640 ms`; it is not relabelled as the plan's waived pressure-first
  85-second proof.
- Natural source AI completed one Hall and one Barracks and launched
  `unit-grunt-29` in source force 1 at tick `4651` with an `attack-move` order.
  The launch was observed by page `140,879 ms`. Contact did not occur before
  closeout; the accepted Plan 014 ordinary/preserved contact evidence remains
  the contact authority under the user override.
- F11/F12 restored the exact paused tick `5019` in the same page, then resumed
  at the unchanged default 45/1.5 pace. Final state was `playing`, unpaused,
  with three Peasants, one Hall, one Farm, one Barracks, and one Footman.
- Final average update/render were `7.007/13.677 ms`, within the shared
  `20/24 ms` budgets. There were no page exceptions or black screenshots; all
  exact server/browser PIDs stopped and port `56281` cleared.

The user explicitly replaced the original exhaustive release gate with this
ordinary integrated acceptance. The 18-run candidate bakeoff/score, nine-seed
repeat matrix, two distinct play sessions, exact contact window, full M01–M13
replay, and independent reviewer were waived and are not claimed as executed.

## Residual risk

No unresolved release blocker remains under the user override. Cross-seed
contact spread and the pressure-first 85-second path retain projected/static or
preserved Plan 014 coverage; rerun the frozen matrix before changing candidate
B's speed or distance band.

## Review decision

- Decision: **READY — DONE**
- Reviewer: user-directed final ordinary-play override, verified by Codex on Halla
- Reason: the clean one-page session proved candidate B's honest pace/band,
  responsive base-to-unit loop, save/load continuity, live performance, and a
  natural land-AI attack launch; the exhaustive matrix is explicitly waived.
