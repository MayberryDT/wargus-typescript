# Implementation Plans

Generated by the improve skill on 2026-06-16 and extended by the plan-optimizer skill on 2026-06-16. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add repository agent guidance | P1 | S | - | DONE |
| 002 | Restore runtime determinism verification | P1 | S | - | DONE |
| 003 | Upgrade Vite out of the vulnerable range | P1 | S | - | DONE |
| 004 | Harden map setup JSON loading against SPA fallback HTML | P1 | S | - | DONE |
| 005 | Stabilize stale fixed-demo verifier contracts | P2 | M | 002 | DONE |
| 006 | Extract a low-risk orders.ts pilot slice | P3 | M | 002, 005 | DONE |
| 007 | Fix browser runtime smoke process cleanup | P1 | S | - | DONE |
| 008 | Include omitted verifiers in the full verify gate | P1 | S | - | DONE |
| 009 | Extract a browser verifier harness pilot | P2 | M | 007 | DONE |
| 010 | Convert fixed-demo speed verifier to a layout contract | P2 | M | 008 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Plan Optimizer Rubric

Each plan was optimized against this 100-point rubric:

- Goal and done criteria (15): high-quality plans state one measurable outcome and machine-checkable completion.
- Repo grounding (15): high-quality plans include current code excerpts, exact paths, and relevant repo conventions.
- Sequencing and dependencies (15): high-quality plans put prechecks, edits, verification, and handoff in a safe order.
- Verification strength (20): high-quality plans name exact commands, expected output, and failure boundaries.
- Risk controls (15): high-quality plans have specific STOP conditions, rollback notes, and out-of-scope boundaries.
- Executor specificity (15): high-quality plans minimize judgment calls for a fresh agent.
- Maintenance value (5): high-quality plans explain what future reviewers should watch.

## Optimized Score Trajectory

| Plan | Score trajectory | Main improvements |
|------|------------------|-------------------|
| 001 | 78 -> 89 -> 93 -> 93 | Inlined required AGENTS content, added source-change guardrails, clarified plan-only execution boundary. |
| 002 | 84 -> 91 -> 94 -> 94 | Added red-baseline precheck, narrowed README verification, added explicit no-random source scan. |
| 003 | 82 -> 90 -> 93 -> 93 | Added registry/network assumptions, lockfile version checks, and production-smoke verification for Vite. |
| 004 | 83 -> 91 -> 94 -> 94 | Added target loader shape, package-script parse checks, and clearer SPA fallback failure handling. |
| 005 | 84 -> 92 -> 95 -> 95 | Added dependency gate on plan 002, stronger package-chain verification, and clearer runtime/static contract split. |
| 006 | 78 -> 89 -> 94 -> 94 | Fixed the internal `orders.ts` import issue, added diff shape checks, and strengthened refactor STOP conditions. |
| 007 | 78 -> 89 -> 95 -> 95 | Added named debug-port drift guard, exact negative/positive cleanup checks, and split live smoke from static viewport verification. |
| 008 | 76 -> 88 -> 94 -> 94 | Turned a vague coverage gap into a four-script package contract with individual verification and cycle STOP conditions. |
| 009 | 70 -> 84 -> 91 -> 93 -> 93 | Narrowed a broad browser-verifier dedupe into a two-script harness pilot with plan 007 as a dependency. |
| 010 | 72 -> 86 -> 92 -> 94 -> 94 | Moved fixed-demo speed-control checks from brittle source fragments to a live modern-HUD layout contract. |

## Dependency Notes

- 005 depends on 002 because the fixed-demo verifier work should run after the runtime determinism gate is green again.
- 006 depends on 002 and 005 because refactoring `src/simulation/orders.ts` should wait until the cheap verification baseline is trustworthy.
- 009 depends on 007 because the shared browser verifier harness must inherit the corrected process cleanup behavior.
- 010 depends on 008 because the modern HUD layout verifier should be part of the full gate before it becomes the primary speed-control contract.

## Verification Baselines From Recon

### Original baseline for plans 001-006

- `./node_modules/.bin/tsc --noEmit` passed.
- `npm run verify:wargus-assets` passed and checked 1182 files.
- `npm run verify:map-setups` passed and checked 301 setups.
- `npm run verify:save-schema` passed.
- `npm run verify:runtime-determinism` failed on `src/wargus/demoScenario.ts:246`.
- `npm run verify:fixed-demo-random-ai` failed on a stale expected source fragment.
- `npm audit --omit=dev --audit-level=high --json` found one high advisory affecting installed `vite@8.0.14`.

### Extended baseline for plans 007-010

- `./node_modules/.bin/tsc --noEmit` passed.
- `npm run verify:wargus-assets` passed and checked 1182 files.
- `npm run verify:runtime-determinism` passed.
- `npm audit --audit-level=high --omit=dev --json` found zero vulnerabilities.

## Findings Considered And Rejected

- Full `orders.ts` rewrite: rejected for now. The file is too central to safely restructure in one pass; plan 006 starts with a small pilot extraction and leaves larger gameplay slices for later plans after the verification baseline is green.
- Replacing all source-fragment verifiers at once: rejected for now. Several static verifiers encode real source-port invariants; plan 005 fixes the known stale contract and sets boundaries for future conversions to behavior checks.
